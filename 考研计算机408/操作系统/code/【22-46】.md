```c
semaphore sem_AC = 0;
semaphore sem_CD = 0;
semaphore sem_CE = 0;

T1() {
    操作A;
    signal(sem_AC);
    wait(sem_CE);
    操作E;
    操作F;
}
T2() {
    操作B;
    wait(sem_AC);
    操作C;
    signal(sem_CE);
    操作D;  // 因为属于同一线程，可以按顺序执行，所以这里就没加信号量了
}