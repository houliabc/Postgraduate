```c
cobegin {
    sema seats = 10;  // 表示空闲座位数
    sema mutex = 1;  // 互斥访问座位
    sema call = 0;  // 表示是否叫号
    process 顾客 i {
        P(seats);
        P(mutex);
        从取号机获取一个号码;
        V(mutex);
        等待叫号;
        P(call);
        获取服务;
    }

    process 营业员 {
        while (TRUE) {
            V(call);
            叫号;
            V(seats);  // 让出空位置
            为客户服务;
        }
    }
} coend
