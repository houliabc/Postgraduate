```c
semaphore sem_AC = 0;
semaphore sem_BC = 0;
semaphore sem_CE = 0;
semaphore sem_DE = 0;

A() {
    操作A
    signal(sem_AC);
}
B() {
    操作B
    signal(sem_BC);
}
C() {
    wait(sem_AC);
    wait(sem_BC);
    操作C
    signal(sem_CE);
}
D() {
    操作D;
    signal(sem_DE);
}
E() {
    wait(sem_CE);
    wait(sem_DE);
    操作E
}
