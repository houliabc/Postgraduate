# 第一章 计算机系统概述 

## 一、冯·诺依曼结构

核心是**存储程序**。计算机由运算器、控制器、存储器、输入、输出五大部件组成。指令和数据均用二进制表示，同存于主存，指令分为操作码和地址码。程序和数据预先存入主存，机器自动逐条执行指令。

运算器加控制器合称CPU。CPU包含数据通路与控制器，还有MAR、MDR用于和主存交互。内存分为主存、Cache，外存为硬盘、固态硬盘。CPU+主存称为主机，依靠三类总线完成部件互联。

联动计算考点：MAR位数对应主存地址空间大小，MDR位数等于存储字长。

## 二、指令执行过程

指令执行分四步：取指令、译码、运算、写回结果。
核心考点：指令和数据都是二进制，计算机靠**读取阶段**区分——取指令周期从主存读出的是指令，执行周期从主存读出的是数据；取指令用PC地址，取数据用操作数地址。
补充时序概念：一个指令周期包含多个机器周期，一个机器周期包含多个时钟周期，时钟周期是CPU最小时间单位。

## 三、系统层次结构

软件分为应用软件和系统软件。
语言分为高级语言、汇编语言、机器语言。对应翻译程序：汇编器翻译汇编语言；编译器将高级语言完整翻译为目标代码再执行；解释器边翻译边执行，不生成完整目标程序。
C语言程序需经过预处理、编译、汇编、链接四步，最终生成可执行文件。

## 四、ISA与微体系结构

ISA（指令集体系结构）是软硬件接口，规定计算机**能做什么**，如x86、ARM，定义了指令格式、寄存器、寻址方式等。
微体系结构是硬件实现方案，决定**怎么做**，软件不可见，比如流水线级数、Cache大小。二者之上是物理电路实现。

## 五、核心性能指标与计算公式（计算题必考点）

### 1. 基础时间指标

- 时钟周期：CPU最小时间单位，即时钟频率的倒数。
  公式：**时钟周期 = 1 / 时钟频率**
  例：时钟频率2GHz，时钟周期 = 1 / 2G = 0.5ns

- CPU执行时间：运行一个程序花费的总时间。
  核心公式：**CPU执行时间 = 指令数 × CPI × 时钟周期**
  等价形式：**CPU执行时间 = (指令数 × CPI) / 时钟频率**
  CPI：每条指令执行所需的平均时钟周期数。

### 2. 平均CPI计算

若程序包含多种指令，CPI为加权平均值：
**平均CPI = Σ(第i类指令条数 × 第i类指令CPI) / 总指令数**
例：100条指令，50条CPI=1，30条CPI=2，20条CPI=3，则平均CPI = (50×1 + 30×2 + 20×3) / 100 = 1.7

### 3. 运算速度指标

- MIPS：每秒百万条指令，衡量整数运算速度。
  公式：**MIPS = 指令数 / (CPU执行时间 × 10⁶) = 时钟频率 / (CPI × 10⁶)**
  注意：MIPS越高速度越快，但不同指令集的机器不能用MIPS直接对比。

- MFLOPS：每秒百万次浮点操作，衡量浮点运算速度。
  公式：**MFLOPS = 浮点操作次数 / (CPU执行时间 × 10⁶)**
  该指标更适合衡量科学计算类程序的性能，比MIPS更准确。

### 4. 阿姆达尔定律

用于计算部件优化后系统的整体加速比：
**系统加速比 = 1 / [(1 - 改进比例) + 改进比例 / 部件加速比]**
核心逻辑：优化占比越大的部件，整体性能提升越明显。

## 六、核心易错点

1. 冯·诺依曼体系核心：存储程序
2. 主机 = CPU + 主存
3. 编译生成目标文件，解释边译边执行
4. 指令与数据依靠读取上下文区分
5. 计算时注意单位统一：频率与时间的换算、纳秒与秒的换算。

# 第二章 总线系统

总线是一组可被多个部件**分时共享**的公共信息传输线路，核心特征是分时复用、多设备共用。优势是简化硬件设计、便于系统扩展、降低成本；缺点是存在传输瓶颈，同一时刻仅允许一个主设备占用，需仲裁机制解决使用权冲突。

## 一、总线分类

### 1. 按所在位置分类

- **片内总线**：芯片内部的连线，用于CPU内部寄存器、ALU、控制器之间的数据交互。
- **系统总线**：主机内部连接CPU、主存、I/O接口的总线，分为三类：
  - 数据线：双向传输数据，位数决定单次传输的数据量
  - 地址线：单向传输地址，位数决定系统可寻址的内存空间大小
  - 控制线：传输控制信号、时序信号、设备状态信号
- **通信总线（外部总线）**：用于计算机与外部设备、计算机与计算机之间的互联，如USB、SATA、以太网。

### 2. 按数据传输方式分类

- **并行总线**：多位数据同时传输，短距离下速度快；长距离易出现时钟偏移、信号串扰，如传统PCI总线。
- **串行总线**：数据逐位依次传输，线路数量少、抗干扰强、适合远距离；高频串行总线的总带宽可超越并行总线，如USB、PCIe、SATA。

## 二、总线性能指标（计算题核心考点）

1. **总线宽度**：数据线的总位数，单位为bit，常见32位、64位。
2. **总线时钟频率**：总线工作的时钟频率，单位MHz/GHz，频率越高传输能力越强。
3. **总线传输周期**：完成一次完整数据传输所需的时钟周期数，通常为时钟周期的整数倍。
4. **总线带宽（最大数据传输率）**：单位时间内总线可传输的最大数据量，常用单位MB/s、GB/s。
   - 基础公式：
     **总线带宽 = (总线宽度 / 8) × 总线工作频率 ÷ 单次传输占用时钟数**
   - 突发传输公式（仅首周期发地址，后续每个时钟传一个数据）：
     **突发传输带宽 = (总线宽度 / 8) × 总线工作频率**
   - 计算示例：32位总线，时钟频率100MHz，一次传输占2个时钟周期，带宽 = (32/8) × 100MHz ÷ 2 = 200MB/s。
5. 总线复用：地址线与数据线分时复用同一组物理线路，可减少引脚数量，但会降低实际传输效率。

## 三、总线仲裁

用于解决多个主设备同时申请总线使用权的冲突，分为集中式和分布式两类。

### 1. 集中式仲裁

- **链式查询**：总线同意信号串行串联所有设备，优先级沿链路固定；硬件最简单，对线路故障敏感。
- **计数器定时查询**：通过计数轮询设备号，优先级可灵活设置，控制线数量中等。
- **独立请求**：每个设备有独立的请求线，响应速度最快、优先级可动态配置；硬件成本最高、控制线数量最多。

### 2. 分布式仲裁

无需中央仲裁器，每个设备自主参与优先级竞争，系统可靠性更高。

## 四、总线定时方式

规定总线传输过程中信号的时序规则，分为四类。

### 1. 同步定时

由统一的时钟信号控制所有传输步骤，每个操作在固定时钟周期内完成。

- 优点：时序规则简单、传输速度快、控制逻辑简洁
- 缺点：必须按最慢设备设计时钟，长距离存在时钟偏移，不适合多速率设备
- 支持突发传输：仅发送一次首地址，后续连续传输多个数据，省去重复发地址的开销，效率大幅提升。

### 2. 异步定时

无统一时钟，依靠请求、应答信号的握手机制完成传输。
三类握手模式：

- 不互锁：请求发出后无需等待应答即可撤销，速度最快、可靠性最低
- 半互锁：请求需等待应答后撤销，应答自行撤销，效率与可靠性兼顾
- 全互锁：请求与应答互相确认，可靠性最高、传输速度最慢

异步串行通信有固定帧格式：起始位 + 数据位 + 校验位 + 停止位；波特率表示每秒传输的二进制位数。

### 3. 半同步定时

保留统一时钟基准，同时增加等待应答信号，允许慢速设备插入等待周期，兼顾同步的速度和异步的兼容性。

### 4. 分离式定时

将总线周期拆分为两个子周期：主设备发送地址命令后立即释放总线，从设备准备数据期间总线可分配给其他设备；数据准备完成后，从设备再申请总线完成传输。

- 核心优势：消除从设备准备数据的总线空闲，**大幅提升总线利用率**
- 缺点：控制逻辑复杂，单次传输的延迟增加。

## 五、总线结构层次

### 1. 单总线结构

所有部件共用一组系统总线，结构简单、扩展方便；所有设备共享总线带宽，传输瓶颈严重，仅适合低速系统。

### 2. 多总线结构

按设备速率分层设计，高速设备靠近CPU，低速设备通过桥接芯片转接。

- 经典南北桥架构：北桥管理CPU、内存、显卡等高速设备；南桥管理硬盘、USB、串口等低速外设。
- 现代架构：内存控制器、PCIe控制器集成进CPU内部，低速外设统一由PCH芯片管理。

## 六、易错点与计算注意

1. 总线仲裁解决**使用权分配**问题，总线定时解决**传输时序配合**问题，二者概念不可混淆。
2. 带宽计算注意单位换算：总线宽度以位为单位，计算字节级带宽需除以8；频率单位需统一（1GHz = 1000MHz）。
3. 突发传输仅第一个数据有地址控制开销，后续数据无额外开销，实际带宽远高于普通单次传输。
4. 串行总线不等于低速，PCIe等高频串行总线的总带宽远超传统并行PCI总线。
5. 分离式定时的核心优化目标是提升总线利用率，代价是控制复杂度上升、单次传输延迟增加。

# 第三章 主存储器 

## 一、存储器分类与层次结构

存储器按功能分为三类：主存可与CPU直接交换信息；辅存作为后援存储大容量暂存数据；Cache用于缓解CPU与主存的速度差。
按存取方式分：随机存取RAM，访问时间与地址无关；顺序存取如磁带，速度最慢；直接存取如磁盘，先定位磁道再连续读写；相联存储器按内容检索，用于Cache标记并行比较。
按断电保存性分：易失性如RAM、Cache；非易失性如ROM、磁盘、闪存。

存储系统核心是两层结构：Cache-主存层解决速度不匹配，硬件自动完成，对程序员透明；主存-辅存层解决容量不足，由硬件和操作系统共同管理，实现虚拟存储。
**核心易错点**：CPU不能直接访问辅存，辅存数据必须先调入主存才能被CPU读取。

## 二、存储芯片基础参数与计算

### 1. 核心容量公式

**存储容量 = 存储单元数 × 存储字长 = 2^地址线条数 × 数据线条数**，单位为bit；换算为字节需除以8。
例：地址线10根、数据线8根的芯片，容量=2^10 ×8bit = 1KB。

### 2. SRAM与DRAM核心对比

| 类型 | 结构      | 刷新         | 读出特性         | 速度 | 集成度     | 用途  |
| :--- | :-------- | :----------- | :--------------- | :--- | :--------- | :---- |
| SRAM | 6管触发器 | 无需刷新     | 非破坏性读出     | 快   | 低、成本高 | Cache |
| DRAM | 单管+电容 | 必须定时刷新 | 破坏性读出需再生 | 较慢 | 高、成本低 | 主存  |

DRAM采用地址引脚复用技术，行、列地址分时输入，由RAS、CAS信号选通，大幅减少芯片引脚。
存储周期>存取时间：DRAM读写后需预充电，连续两次操作的最小间隔长于单次存取耗时。

### 3. ROM与闪存

ROM分类：掩模ROM出厂固化不可改；PROM仅一次编程；EPROM紫外线全片擦除；EEPROM电可擦除、支持按字读写。
Flash闪存：非易失，读速接近RAM，写入较慢，以**页为单位读写、块为单位擦除**；U盘、固态硬盘均为Flash介质，不属于传统ROM。

## 三、DRAM刷新机制与计算

刷新按行进行，无需列地址，典型刷新周期为64ms。
三种刷新方式：

1. 集中刷新：固定时间段内逐行刷完，存在较长死时间，系统效率低。
   **死时间 = 行数 × 单个存储周期**
2. 分散刷新：每个存储周期拆分为读写+刷新两段，无死时间，但存储周期翻倍，整体速度下降。
3. 异步刷新：均匀分配刷新间隔，死时间仅一个存储周期，兼顾效率，是主流方案。
   **刷新间隔 = 刷新总周期 / 总行数**
   例：1024行、64ms刷新周期，刷新间隔=64ms/1024=62.5μs，即每隔62.5μs刷新一行。

## 四、主存扩展与编址计算

### 1. 三种扩展方式

- **位扩展**：增加存储字长，地址线全部并联，数据线按位拼接。
- **字扩展**：增加存储单元数量，数据线并联，高位地址经译码器产生片选信号。
- **字位扩展**：同时扩展数量与位宽，实际场景最常用。
  **芯片总数公式：总存储容量 / 单芯片容量**

### 2. 编址方式

- 按字节编址：每个字节对应一个地址，存储单元为8位，是主流计算机的编址方式。
- 按字编址：每个存储字对应一个地址，存储单元等于存储字长。
  **核心关系**：MAR位数决定可寻址的地址空间大小；MDR位数等于存储字长。

## 五、多模块存储器与流水线访问计算

多模块存储器包含多个独立存储体，可并行工作，通过流水线访问提升带宽。

- 连续编址（高位交叉）：高位做体号，连续地址在同一个体内，访问连续数据无提速效果。
- 低位交叉编址：低位做体号，连续地址分布在不同存储体，流水线方式访问，带宽显著提升。

### 核心计算公式

n体低位交叉，单个存储体存取周期为T，总线传输周期为τ，满足T = nτ时流水线效率最高。
**连续读取m个字的总耗时 = T + (m-1)τ**
**最大带宽 = n × 存储字长 / T**

## 六、局部性原理

分为两类：

- 时间局部性：被访问过的数据，短时间内会被再次访问。
- 空间局部性：被访问的数据附近的数据，也会很快被访问。
  程序指令、顺序数组访问具备良好的空间局部性；步长越小空间局部性越好，循环体越小、迭代次数越多时间局部性越好。
  局部性是存储器层次结构的核心依据，上层缓存下层的数据块，兼顾速度与成本。

## 七、Cache映射方式与地址拆分计算

主存与Cache以固定大小的**块**为单位交换数据，Cache行与主存块大小一致。
地址从高位到低位依次为：标记位 + 行号/组号位 + 块内地址位。

### 1. 直接映射

主存块只能放入固定的Cache行，映射规则：**行号 = 主存块号 % Cache总行数**。
地址结构：标记位 + 行号位 + 块内地址位。

- 块内地址位数 = log₂(块大小)
- 行号位数 = log₂(Cache总行数)
- 标记位数 = 总地址位数 - 行号位数 - 块内地址位数
  特点：实现简单，冲突概率高，命中率低。

### 2. 全相联映射

主存块可放入任意Cache行，地址结构：标记位 + 块内地址位。
特点：冲突最低、命中率最高；需全量并行比较标记，硬件成本高，**仅适合小容量Cache。**

### 3. 组相联映射

组间直接映射，组内全相联。**N路组相联即每组包含N个Cache行**。
**组数 = Cache总行数 / 每组路数**
地址结构：标记位 + 组号位 + 块内地址位。

- 组号位数 = log₂(组数)
  特点：兼顾速度与命中率，是现代CPU的主流方案。

## 八、Cache替换算法与写策略

### 1. 常用替换算法

- FIFO先进先出：替换最早装入的块，实现简单，不符合局部性，命中率低。
- LRU最近最少使用：替换最久未被访问的块，符合局部性，命中率高，通过计数器硬件实现。

### 2. 写策略

- **全写法（写直达）**：写命中时同时更新Cache和主存，可靠性高，写开销大，通常搭配写缓冲队列。
- **回写法**：写命中时只更新Cache，块被替换时才写回主存；增加脏位标记修改状态，写效率高，存在数据一致性问题。
  写不命中时，**写分配法先加载块到Cache再写入主存；非写分配法直接写入主存**，不加载到Cache。

## 九、Cache性能计算公式（核心考点）

### 1. 基础性能公式

命中率h：CPU访问Cache命中的比例；缺失率=1-h。
**平均访问时间 = 命中时间 + 缺失率 × 缺失损失**
等价形式：**平均访问时间 = h×命中时间 + (1-h)×缺失损失**
缺失损失：Cache缺失后，从主存读取块并填充到Cache的总耗时。

### 2. 访问效率

**Cache访问效率 = 命中时间 / 平均访问时间 × 100%**

### 3. 多级Cache

现代CPU采用三级Cache：L1分为指令Cache和数据Cache，单核私有；L2为单核私有统一Cache；L3为多核共享统一Cache。

- 局部缺失率：单级Cache的缺失比例。
- 全局缺失率：所有层级Cache全部缺失的比例，数值远小于局部缺失率。

## 十、核心易错点与计算注意

1. **计算地址位数时，块大小默认单位为字节，需先统一单位再取对数**。
2. **字扩展用高位地址做片选；低位交叉编址用低位做体号**，二者地址位用途不可混淆。
3. 缺失损失通常以时钟周期为单位，计算平均访问时间时需与命中时间单位统一。
4. 数组按行优先存储时，逐行访问空间局部性好；逐列访问步长大，空间局部性差，缺失率高。
5. 主存块地址对齐时，占用的主存块更少，Cache命中率更高。

------

# 第四章 数据的表示与运算

## 一、数制与编码基础

### 1. 二进制核心逻辑

- **计算机采用二进制：物理器件易实现、运算规则简单、适配布尔逻辑电路**
- 编码本质：同一串二进制位，解释规则不同则含义完全不同，可表示整数、浮点数、字符、指令等
- 进制转换核心方法：
  - 转十进制：按权展开求和
  - 十进制**整数**转二进制：除基取余，**逆序排列**
  - 十进制**小数**转二进制：乘基取整，顺序排列；**并非所有十进制小数都能被二进制精确表示**
- 十六进制：1位对应4位二进制，用于简化书写；按4位分组转换，不足补0

### 2. 常用数值速记

- 8位无符号最大值255，16位无符号最大值65535
- **补码最小值对应0x80开头，最大值对应0x7F开头**
- 2的幂次转十六进制：n = i + 4j，首位为1/2/4/8对应i=0/1/2/3，后接j个0

## 二、整数编码体系

### 1. 无符号整数

- 所有位均为数值位，取正权
- n位范围：**0 ~ 2ⁿ - 1**
- 典型应用：内存地址、指针运算

### 2. 有符号四种编码对比

| 编码 | 符号位规则                       | n位数值范围        | 0的表示        | 核心特点                                           |
| :--- | :------------------------------- | :----------------- | :------------- | :------------------------------------------------- |
| 原码 | 最高位0正1负，**数值位为绝对值** | -(2ⁿ⁻¹-1) ~ 2ⁿ⁻¹-1 | 两种（+0、-0） | **乘除方便，加减复杂**，直观易读                   |
| 反码 | 正数同原码，**负数数值位取反**   | -(2ⁿ⁻¹-1) ~ 2ⁿ⁻¹-1 | 两种           | 仅作原补转换中间态，**加法需循环进位**             |
| 补码 | 最高位取负权，0正1负             | **-2ⁿ⁻¹ ~ 2ⁿ⁻¹-1** | 唯一全0        | **减法转加法，符号位参与运算**，现代计算机默认采用 |
| 移码 | 真值+偏置值（通常2ⁿ⁻¹）          | -2ⁿ⁻¹ ~ 2ⁿ⁻¹-1     | 唯一           | 无符号特性，便于大小比较，专用于浮点数阶码         |

### 3. 补码核心考点

- **转换技巧**：负数补码 = 绝对值二进制取反 + 1；原补互转通用法：符号位不变，数值位取反加1；快速法：从右往左找第一个1，其左侧数值位取反
- **补码速算**：高位连续的1可合并为最左侧一个1，数值不变
- **核心易错**：补码范围不对称，负数比正数多一个；**最小负数无对应正数值，取反会溢出**

### 4. 移码核心性质

- **与补码仅符号位相反**，数值位完全相同
- **按无符号规则比较大小，结果与真值大小一致，因此用于浮点数阶码简化比较电路**

## 三、类型转换与扩展截断

### 1. 有符号↔无符号转换

- 规则：**机器位模式不变，仅解释方式改变**
- n位转换公式：
  - **有符号v转无符号：v<0时，结果 = v + 2ⁿ**；v≥0时数值不变
  - **无符号转有符号：数值>2ⁿ⁻¹-1时，结果 = 数值 - 2ⁿ**；否则不变
- **C语言隐式陷阱**：混合运算时，有符号数隐式转为无符号数，易出现反常识结果

### 2. 位扩展

- 零扩展：无符号数扩位，高位补0，数值不变
- 符号扩展：有符号数扩位，高位补符号位，数值不变
- **C语言转换顺序：先做位宽扩展，再做符号类型转换**

### 3. 位截断

- 长类型转短类型，直接丢弃高位
- 截断后按目标类型规则重新解释位模式，大概率改变数值

## 四、定点数与浮点数

### 1. 定点数基础

- 定点整数：小数点约定在最右侧
- 定点小数：小数点约定在符号位右侧
- n位补码定点小数范围：**-1 ~ 1 - 2^-(n-1)**

### 2. 浮点数基础

- 组成：符号位 + 阶码（指数） + 尾数
- **规格化：尾数最高位为1，保证表示唯一、精度最大化**
- **左规：尾数左移，阶码减1；右规：尾数右移，阶码加1**

### 3. IEEE 754 标准

#### 单精度float（32位）

- 结构：1位符号S + 8位阶码E（移码，偏置127） + 23位尾数M（原码，隐藏位1）
- 真值公式：**(-1)^S × 1.M × 2^(E-127)**

#### 双精度double（64位）

- 结构：1位符号S + 11位阶码E（移码，偏置1023） + 52位尾数M（原码，隐藏位1）
- 真值公式：**(-1)^S × 1.M × 2^(E-1023)**

#### 数值分类（单精度为例）

1. **规格化数**：阶码非全0非全1，指数范围-126~127，尾数含隐藏位1
   - **最小规格化正数：2\^-126；最大规格化正数：(2-2\^-23)×2^127**
2. **非规格化数**：阶码全0，尾数非全0，**指数固定为-126**，无隐藏位
   - 最小非规格化正数：2^-149；实现0到最小规格化数的平滑过渡
3. **特殊值**：
   - 阶码全1、尾数全0：正负无穷大
   - 阶码全1、尾数非0：NaN非数值
4. **机器零**：阶码全0且尾数全0

#### 溢出判断

- **上溢：指数超过最大值**，结果为±∞，属于**异常需中断**
- 下溢：指数小于最小值，结果趋于0，按机器零处理

### 4. C语言浮点类型转换

- int→float：不溢出但有效位不足，可能舍入丢失精度
- int/float→double：范围精度均足够，精确保留
- double→float：可能溢出变无穷，也可能舍入丢精度
- 浮点转int：可能溢出，小数部分直接截断丢弃
- **易错点**：**浮点数存在大数吃小数现象，加法不满足结合律**

## 五、字节顺序与内存对齐

### 1. 大小端字节序

- **大端模式**：最高有效字节存低地址，符合人类阅读习惯
- **小端模式**：最低有效字节存低地址，x86架构默认
- 判断方法：查看多字节数据低地址存放的是高位还是低位字节

### 2. 内存对齐

#### 对齐原因

- 主存按存储字长批量读取，非对齐数据需多次访存，降低性能
- 空间换时间，牺牲少量内存换取访问效率

#### 对齐规则（计算考点）

1. 有效对齐数 = min(变量自身大小，系统默认对齐数)
   - 32位系统默认4字节，64位默认8字节
2. **成员对齐**：每个成员起始地址必须是自身有效对齐数的整数倍，不足则填充字节
3. **整体对齐**：结构体总大小必须是整体有效对齐数（内部最大成员对齐数与系统对齐数的较小值）的整数倍，末尾填充
4. 数组成员按元素类型对齐

## 六、基本运算：位运算、逻辑运算、移位运算

### 1. 按位运算

- 按位与&：提取指定位、清0特定位
- 按位或|：置1特定位
- 按位异或^：翻转特定位；与全1异或等价取反
- 按位取反~：所有位翻转

### 2. 逻辑运算

- &&、||、!，非0为真，0为假，结果只能是0或1
- **短路特性**：第一个操作数可确定结果时，不执行第二个操作数

### 3. 移位运算

- **逻辑移位**：对应无符号数；左移高位丢、低位补0；右移低位丢、高位补0
- **算术移位**：对应有符号补码；左移同逻辑左移；右移低位丢、高位补符号位
- 左移1位等价乘2，右移1位等价除2（向下取整）
- **溢出判断**：
  - 逻辑左移：移出高位为1则溢出
  - 算术左移：移出位与符号位不同则溢出
- **易错点**：移位位数≥数据位宽属于未定义行为

## 七、整数加减运算与标志位

### 1. 加法器基础

- 全加器：输入本位Xi、Yi和低位进位Ci-1，输出本位和Fi、本位进位Ci
- 串行进位加法器：结构简单，进位延迟随位数线性增长
- 并行进位（先行进位）：提前算出所有进位，并行求和，速度大幅提升
- 加减统一电路：Sub控制信号为1时，Y取反且初始进位为1，实现X-Y的补码运算

### 2. 四大标志位（计算考点）

| 标志 | 全称          | 含义            | 适用场景 | 判断规则                                                     |
| :--- | :------------ | :-------------- | :------- | :----------------------------------------------------------- |
| ZF   | 零标志        | 结果是否为0     | 通用     | 结果全0则ZF=1                                                |
| SF   | 符号标志      | 结果正负        | 有符号数 | 等于结果最高位                                               |
| CF   | 进位/借位标志 | 无符号溢出/借位 | 无符号数 | CF = 最高位进位异或减法控制位；加法CF=1表示溢出，减法CF=1表示不够减 |
| OF   | 溢出标志      | 有符号溢出      | 有符号数 | OF = 最高数值位进位 ^ 符号位进位；OF=1表示溢出               |

### 3. 溢出判断三种方法

1. 硬件进位法：**OF = Cn-1 ^ Cn**，最高数值位进位异或符号位进位
2. 符号位法：同号相加结果符号与加数不同则溢出；异号相加不可能溢出
3. 双符号位法（变形补码）：00正、11负；01正溢出、10负溢出；**OF = S1 ^ S2**

### 4. 整数大小比较

- 无符号数比较：看CF和ZF；小于则CF=1，大于则CF=0且ZF=0
- 有符号数比较：看SF、OF、ZF；小于则SF^OF=1，大于则SF^OF=0且ZF=0

## 八、整数乘除运算

### 1. 原码一位乘法

- 符号位单独异或，数值位按无符号数相乘
- 流程：部分积初始为0，逐位判断乘数最低位；为1则加被乘数，然后部分积与乘数整体逻辑右移；循环n次
- 结果：得到2n位乘积

### 2. 补码一位乘法（Booth算法）

- 符号位参与运算，直接得到补码乘积
- 规则：乘数末尾增设辅助位Y-1=0，根据YiYi-1判断：01加X补，10减X补，00/11不加不减；然后整体算术右移一位，循环n次
- 结果：2n位补码乘积

### 3. 阵列乘法器

- 全硬件并行实现，单周期完成乘法，速度远快于移位迭代法
- 结构规整，适合超大规模集成电路实现

### 4. 原码除法（加减交替法/不恢复余数法）

- 核心口诀：正、1、减；负、0、加
- 结构：余数寄存器R + 商寄存器Q，整体左移，上商在Q最低位
- 恢复余数法：不够减时加回除数，速度不均匀；不恢复余数法根据余数符号决定下次加减，速度均匀

### 5. 补码除法

- 符号位参与运算，商符自然形成
- 根据余数与除数的符号关系决定加减，上商规则与原码不同

### 6. 乘除溢出判断

- 无符号乘法：乘积高n位非全0则溢出
- 补码乘法：高n位不全与低n位最高位相同则溢出
- 除法一般不溢出；特殊情况：补码最小负数除以-1时必然溢出

### 7. 编译器优化

- 乘常数：拆分为移位+加减组合，替代乘法指令
- 除2的幂：无符号用逻辑右移；有符号负数需先加偏移量再算术右移，实现朝零取整

## 九、浮点数运算与舍入

### 1. 四种舍入策略

1. **就近舍入到偶数（默认）**：类似四舍五入，中间值取末位为偶数的结果；二进制中0为偶、1为奇
2. 朝+∞方向：总是取更大的可表示数
3. 朝-∞方向：总是取更小的可表示数
4. 朝0方向（截断）：直接丢弃多余位，绝对值变小

### 2. 浮点数加减运算六步（大题考点）

1. **十进制转IEEE754规格化形式**，还原隐藏位1
2. **对阶**：小阶向大阶看齐，阶小的尾数右移，阶差等于右移位数
   - 保留附加位：保护位、舍入位、粘位（右侧有非0则置1），减少精度损失
3. **尾数加减**：附加位共同参与定点加减运算
4. **规格化**：
   - 尾数溢出→右规：尾数右移1位，阶码+1
   - 尾数高位为0→左规：尾数逐次左移，阶码逐次减1
5. **舍入处理**：按舍入策略将尾数截断到指定位数
6. **溢出判断**：右规可能导致阶码上溢（真溢出）；左规可能导致阶码下溢（按机器零处理）

### 3. 核心易错点

- 尾数溢出不等于浮点数溢出，右规即可纠正；**阶码溢出才是真正溢出**
- 对阶必须小阶向大阶对齐，反之会丢失高位有效数字，严重损失精度

# 第五章：指令系统

## 指令集体系结构\(ISA)

### 核心定义

- **指令集**：一台计算机能执行的全部机器指令的集合，是硬件功能的体现。
- **ISA\(指令集体系结构）**：定义机器级程序的格式与行为，是软件与硬件之间的接口，规定了处理器状态、指令格式、指令对状态的影响规则。

### ISA的核心规定

1. **处理器状态\(软件可见）**：通用寄存器、程序计数器PC、程序状态字/标志寄存器、栈指针等；硬件内部的缓存、流水线寄存器等不可见状态不在ISA定义范围内。
2. **指令格式**：指令总长度、操作码与地址码的编码结构、寻址方式规则。
3. **指令行为**：每条指令执行后对寄存器、内存的状态改变规则；硬件可采用流水线、乱序执行等技术，但最终结果必须与顺序执行模型完全一致。

### 两大分类

- **CISC\(复杂指令集）**：指令数量多、长度可变、寻址方式丰富，代表为x86。
- **RISC\(精简指令集）**：指令数量少、定长编码、Load/Store架构，代表为MIPS、ARM、RISC-V。

---

## MIPS 指令系统

\(典型RISC架构，32位定长指令，仅Load/Store指令可访存）

### 寄存器组

共32个32位通用寄存器，核心约定：

- **$0($zero）**：值恒为0，用于简化指令实现。
- **$v0~$v1\(2~3号）**：存放函数返回值。
- **$a0~$a3\(4~7号）**：传递函数前4个参数，超出参数通过栈传递。
- **$t0-$t9**：临时寄存器，调用者负责保存，被调用函数可直接覆盖。
- **$s0-$s7**：保存寄存器，被调用者负责保存，使用前需先入栈。
- **$sp\(29号）**：栈指针，指向栈顶。
- **$fp\(30号）**：帧指针，指向当前栈帧基址。
- **$ra\(31号）**：保存函数返回地址。
- **Hi/Lo**：乘商专用寄存器，用于存放64位乘积或除法的商与余数，采用隐含寻址。

### 指令格式

#### R型\(寄存器运算类）

| op(6位) | rs(5位) | rt(5位) | rd(5位) | shamt(5位) | funct(6位) |
| ------- | ------- | ------- | ------- | ---------- | ---------- |

- 操作码op恒为0，具体功能由funct功能码指定。
- 适用：算术逻辑运算、移位、乘除、`jr`寄存器跳转。

#### I型\(带立即数/偏移类）

| op(6位) | rs(5位) | rt(5位) | immediate(16位) |
| ------- | ------- | ------- | --------------- |

- immediate为16位补码，运算时需符号扩展或零扩展为32位。
- 适用：`lw/sw`数据传送、立即数运算、`beq/bne`分支指令。

#### J型\(无条件跳转类）

| op(6位) | address(26位) |
| ------- | ------------- |

- 跳转地址计算：PC高4位拼接26位地址，再左移2位补0。
- 适用：`j`直接跳转、`jal`跳转并链接。

### 核心指令

#### 数据传送指令\(唯一可访问内存的指令）

- `lw $rt, offset($rs)`：从内存地址`R[$rs] + offset`加载一个字\(4字节）到寄存器。
- `sw $rt, offset($rs)`：将寄存器中的一个字存入指定内存地址。
- 寻址方式：**基址寻址**，$rs为基址寄存器，offset为偏移量。

#### 算术逻辑指令

- 寄存器运算：`addu`、`subu`、`and`、`or`等，均为R型。
- 立即数运算：`addiu`、`andi`等，为I型。
- 移位指令：`sll`\(逻辑左移）、`srl`\(逻辑右移）、`sra`\(算术右移）。
- 乘除指令：结果默认存入Hi/Lo寄存器，通过`mfhi`/`mflo`读取。

#### 控制流指令

- 条件分支：`beq`\(相等跳转）、`bne`\(不等跳转），采用**相对寻址**，偏移量为指令条数。
- 比较指令：`slt`\(小于则置1），配合分支指令实现条件判断。
- 无条件跳转：`j`\(直接跳转）、`jal`\(跳转并保存返回地址到$ra，用于函数调用）、`jr`\(寄存器跳转，用于函数返回）。

### 栈与过程调用

1. 栈从高地址向低地址增长，$sp始终指向栈顶。
2. 每个函数对应一个栈帧，fp固定指向栈帧基址，sp随栈操作动态移动。
3. 调用流程：
   - `jal`指令：保存返回地址到$ra，跳转至目标函数。
   - 函数入口：分配栈帧、保存需保留的寄存器。
   - 函数返回：恢复寄存器、释放栈帧，通过`jr $ra`跳转回调用点。
4. 栈帧内容：返回地址、旧帧指针、被调用者保存寄存器、局部变量、溢出参数。

### 程序结构实现

- 分支语句：通过比较指令+分支指令实现if-else逻辑。
- 循环语句：do-while先执行后判断；while循环分「先跳转到条件判断」和「先判断后执行」两种编译模式。
- 数组访问：基地址+下标×元素大小计算地址，采用**变址寻址**实现遍历。

---

## x86-64 指令系统

\(典型CISC架构，变长指令，支持直接对内存操作数运算）

### 寄存器组

共16个64位通用寄存器，核心约定：

- **rax**：存放函数返回值。
- **rdi、rsi、rdx、rcx、r8、r9**：传递函数前6个参数。
- **rsp**：栈指针。
- **rbp**：帧指针。
- **rip**：指令指针，即程序计数器PC。
- **标志寄存器**：包含CF\(进位）、ZF\(零标志）、SF\(符号）、OF\(溢出）四个核心条件码。
- 支持按低32位、低16位、低8位分段访问寄存器。

### 核心指令

#### 数据传送指令

- `push`/`pop`：栈操作，自动调整rsp的值。
- `mov`：通用数据复制，支持寄存器↔寄存器、立即数→寄存器/内存、寄存器↔内存；**不支持内存直接到内存的传送**。
- `lea`：加载有效地址，也可用于高效算术运算\(将乘法转换为移位+加法）。

#### 算术逻辑指令

- 一元运算：`inc`、`dec`、`neg`、`not`，操作数兼作源和目的。
- 二元运算：`add`、`sub`、`and`、`or`、`xor`、`imul`等，第一个操作数同时作为目的。
- 移位指令：`sal/shl`\(左移）、`sar`\(算术右移）、`shr`\(逻辑右移）。
- 乘除：单操作数乘除结果存于`rdx:rax`；`idiv`为有符号除法，`cdq`用于符号扩展。
- `cmp`/`test`：仅设置条件码，不修改操作数本身。

#### 控制流指令

- 条件跳转：`je`、`jne`、`jg`、`jl`、`ja`、`jb`等，根据条件码判断跳转，采用PC相对寻址。
- 无条件跳转：`jmp`。

### 过程调用

- `call`指令：将返回地址压栈，跳转至目标函数。
- `ret`指令：弹出栈顶返回地址到rip，实现返回。
- 典型栈帧结构：`push rbp` → `mov rbp, rsp` → `sub rsp, 空间大小` → 函数体 → `leave` → `ret`。
- 前6个参数通过寄存器传递，超出部分压入栈中。

---

## 指令格式设计

### 指令长度与地址码

#### 定长 vs 变长指令

| 类型     | 优点                           | 缺点                         | 代表架构     |
| -------- | ------------------------------ | ---------------------------- | ------------ |
| 定长指令 | 规整，取指译码简单，执行速度快 | 存在编码冗余，浪费存储空间   | MIPS\(RISC） |
| 变长指令 | 编码紧凑，节省存储空间         | 取指译码复杂，硬件实现成本高 | x86\(CISC）  |

#### 地址码分类

按地址码数量从多到少分为：四地址→三地址→二地址→一地址→零地址。

- 地址码越少，单条指令的直接寻址范围越大，但完成同一功能所需指令条数可能增加。
- 减少地址码的常用方法：隐含寻址、PC隐含下条指令地址、中间结果暂存寄存器。

### 扩展操作码

#### 核心思想

**操作码长度随地址码数量减少而增加，短操作码用于多地址指令，长操作码用于少地址指令；每级保留部分编码作为扩展标志。**

#### 经典计算模型

以16位指令、4位操作码、3个4位地址码为例：

- 三地址指令：15条，保留1个编码用于扩展。
- 二地址指令：15条，保留1个编码用于扩展。
- 一地址指令：15条，保留1个编码用于扩展。
- 零地址指令：16条。
- 总计可编码61条指令。

#### 通用计算规则

**零地址指令最大条数 = 剩余扩展码数量 × 2^\(末级地址码位数）。**

---

## 寻址方式

寻址方式是确定操作数有效地址或下一条指令地址的方法。

### 核心寻址方式汇总

1. **立即数寻址**：指令直接给出操作数，无需访存，执行速度快，但操作数大小受限于指令长度。
2. **寄存器直接寻址**：地址码为寄存器编号，操作数在寄存器中，指令短、速度快。
3. **主存直接寻址**：地址码就是操作数的有效内存地址，寻址范围受地址码位数限制。
4. **寄存器间接寻址**：寄存器中存放操作数的有效地址，需一次访存获取操作数。
5. **基址寻址**：有效地址 = 基址寄存器值 + 偏移量；基址可变、偏移固定，适合程序重定位、栈帧访问。
6. **相对寻址**：有效地址 = PC值 + 偏移量；基址隐含为PC，适合转移指令和位置无关代码。
7. **变址寻址**：有效地址 = 变址寄存器值 + 基准地址；基准固定、偏移动态变化，适合数组遍历。
8. **隐含寻址**：操作数地址隐含在操作码中，无需显式给出，如乘除隐含Hi/Lo、栈操作隐含栈顶。

### 基址寻址 vs 变址寻址

| 对比维度 | 基址寻址               | 变址寻址                 |
| -------- | ---------------------- | ------------------------ |
| 固定项   | 偏移量\(指令给出）     | 基准地址                 |
| 变化项   | 基址寄存器的值         | 变址寄存器的值\(偏移量） |
| 典型场景 | 程序重定位、栈内存访问 | 数组遍历、循环下标访问   |

> 基址、相对、变址统称为**偏移寻址**，有效地址均为「寄存器值 + 偏移量」的形式。

---

## CISC 与 RISC 对比

| 特性         | CISC                   | RISC                                   |
| ------------ | ---------------------- | -------------------------------------- |
| 指令数量     | 多\(数百条）           | 少\(通常<100条）                       |
| 指令长度     | 可变长度               | 固定长度                               |
| 寻址方式     | 丰富多样               | 简单，以基址+偏移为主                  |
| 访存规则     | 运算指令可直接访问内存 | 仅Load/Store指令访存，运算仅操作寄存器 |
| 条件码       | 有专用条件码           | 通常无条件码，用显式测试指令           |
| 通用寄存器   | 数量较少               | 数量较多\(通常32个）                   |
| 控制方式     | 多采用微程序控制       | 多采用硬连线控制                       |
| 流水线适配性 | 较差                   | 优秀                                   |
| 代表架构     | x86                    | MIPS、ARM、RISC-V                      |

---

## ISA设计核心要点

1. **状态定义**：明确软件可见的寄存器、内存、条件码范围。
2. **指令集设计**：确定操作码种类、支持的数据类型、指令格式、寻址方式。
3. **异常处理**：规定指令执行异常、外部中断的处理机制。
4. **设计原则**：指令尽量短、预留足够操作码空间、编码唯一可译、指令长度为字节整数倍、指令格式尽量规整。

# 第六章 中央处理器（CPU）

## 6.1 CPU 的基本功能和组成
### 6.1.1 CPU 的基本功能
CPU核心职能是**取指令、译码指令、执行指令**，具体包含：
1. **指令控制**：控制程序执行顺序，完成取指、PC自增、跳转等操作。
2. **操作控制**：生成每条指令对应的部件控制信号，驱动硬件完成操作。
3. **时间控制**：提供时序节拍，保证操作按时间顺序有序执行。
4. **数据加工**：通过ALU完成算术、逻辑运算等数据处理。
5. **异常处理**：响应中断、异常、系统调用等事件。

### 6.1.2 CPU 的基本组成
CPU由**运算器**和**控制器**两大部分组成：
- **运算器（数据通路核心）**：ALU、通用寄存器组、状态/标志寄存器、暂存寄存器、MAR（存储器地址寄存器）、MDR（存储器数据寄存器）。
- **控制器（指挥中心）**：程序计数器PC、指令寄存器IR、控制单元CU、时序系统、中断控制逻辑。

---

## 6.2 单总线 CPU
### 6.2.1 数据通路和控制单元
- **数据通路**：CPU内部各功能部件通过总线连接，形成数据传输与运算的路径，是指令执行的硬件载体，负责数据的传送、运算、存取。
- **控制单元（CU）**：输入为指令操作码、时序信号、状态标志，输出所有部件的控制信号，指挥数据通路按指令要求完成操作。

### 6.2.2 单总线 CPU 执行指令过程
一条指令的执行分为若干工作周期，典型流程：
1. **取指周期**：PC→MAR → 发读存命令 → M(MAR)→MDR → MDR→IR → PC自增
2. **间址周期**（若有间接寻址）：计算有效地址，再次访存获取操作数真实地址
3. **执行周期**：根据指令类型完成运算、访存、跳转等具体操作
4. **中断周期**（若有中断）：**保存断点、关中断、跳转中断服务程序入口**

单总线结构下，同一时刻总线只能传输一路数据，ALU输入端需配合暂存器（Y、Z寄存器）完成运算。

### 6.2.3 数据通路的组成部件
属于数据通路：ALU、通用寄存器、状态寄存器、MAR、MDR、系统总线、暂存寄存器、数据选择器。
> 控制单元CU不属于数据通路，是控制信号的产生者。

### 6.2.4 数据通路中的状态转移
指令执行按**节拍（时钟周期）**逐步推进，每个节拍完成一组微操作；从取指到执行的过程，对应数据通路的状态按节拍有序转移，由控制单元按时序驱动。

### 6.2.5 组合逻辑控制单元（硬布线控制器）
- 原理：用组合逻辑门电路直接生成所有控制信号，输入为指令操作码、时序节拍、状态标志，输出为各部件控制电平。
- 特点：**速度快**，无访存开销；设计复杂、修改/扩展难度大，指令集越复杂设计成本越高。

### 6.2.6 微程序控制单元
- 核心思想：将一条机器指令拆解为一系列微操作，每条微操作对应一条**微指令**，所有微指令组成微程序，预先存入**控制存储器（CM，ROM）**；执行机器指令时，逐条取出微指令并产生控制信号。
- 核心概念：
  - 微命令：最小的控制信号单位
  - **微操作：微命令对应的硬件操作**
  - 微指令：包含多个可并行的微命令
  - 控制存储器：存放全部微程序，属于CPU内部，速度远快于主存
  - μAR（微地址寄存器）、μIR（微指令寄存器）

### 6.2.7 微程序的转移控制
解决微程序的跳转与入口定位问题：
1. **微程序入口映射**：根据机器指令的操作码，映射出对应微程序的起始微地址。
2. **后继微地址生成**：
   - 计数器方式：μAR自动递增，遇到转移指令时修改μAR
   - 断定方式：微指令中直接给出下址字段，配合条件实现分支转移
3. **条件微转移**：根据标志位、指令状态实现微程序内的条件分支。

### 6.2.8 微指令格式
#### 按格式分类
1. **水平型微指令**：微命令字段长，可同时发出多个微命令，并行能力强；微程序短、微指令长，执行效率高。
2. **垂直型微指令**：类似机器指令格式，微指令短、编码紧凑；并行能力弱，微程序长。

#### 微命令编码方式
- 直接编码：每位对应一个微命令，并行最强，微指令最长
- 字段直接编码：将相斥微命令分在同一字段，字段译码输出微命令，压缩长度
- 字段间接编码：一个字段的译码需另一个字段辅助，进一步压缩
- 直接控制：部分微命令直接输出，无需译码

### 6.2.9 硬布线控制器 VS 微程序控制器
| 对比维度   | 硬布线控制器               | 微程序控制器           |
| ---------- | -------------------------- | ---------------------- |
| 执行速度   | 快，纯硬件电路             | 慢，需访问控制存储器   |
| 设计复杂度 | 复杂，指令集越复杂越难设计 | 规整，设计难度低       |
| 修改扩展   | 极难，需重新设计电路       | 容易，只需修改控存内容 |
| 指令适配   | 适合简单指令集（RISC）     | 适合复杂指令集（CISC） |
| 应用场景   | 高性能CPU                  | 传统CISC CPU、单片机   |

---

## 6.3 多总线 CPU
### 6.3.1 ALU 的设计和实现
- 核心功能：完成算术运算（加减、乘除）、逻辑运算（与或非、异或）、移位、比较等操作。
- 进位结构：行波进位（结构简单、速度慢）、先行进位（CLA，速度快、结构复杂）。
- ALU控制：根据ALU操作码选择运算功能，由控制单元根据指令生成。

### 6.3.2 ~ 6.3.5 单周期 CPU 数据通路
单周期CPU：**一条指令在一个时钟周期内完成全部执行**，时钟周期由最慢的指令决定。
#### 按指令类型的通路扩展
1. **R型指令通路**：寄存器堆读两个源操作数 → ALU运算 → 结果写回寄存器堆。
2. **访存指令（lw/sw）通路**：增加数据存储器，基址寄存器+偏移量计算地址，完成内存读写。
3. **跳转/分支指令通路**：增加分支比较逻辑、目标地址计算电路，实现PC的跳转更新。
#### 主控制单元
根据指令操作码，生成所有控制信号（寄存器写使能、ALU功能选择、存储器读写、PC来源选择等）。
#### 单周期特点
- 优点：结构简单，无状态复用。
- 缺点：时钟周期过长（由最慢的lw指令决定），硬件利用率低，性能差。

### 6.3.6 多周期 CPU 的优势
将指令执行拆分为**多个时钟周期**，每个周期完成一个执行阶段：
1. 时钟周期可缩短，按单阶段最慢操作定周期。
2. 硬件资源可复用（ALU既算地址又做运算，指令和数据共用存储器）。
3. 不同指令的周期数不同，短指令无需等待长指令的周期，整体性能显著高于单周期。

### 6.3.7 ~ 6.3.8 多周期 CPU 数据通路
经典5阶段划分，每个阶段一个时钟周期：
1. **取指阶段（IF）**：从指令存储器取指令，PC自增。
2. **译码/取数阶段（ID）**：指令译码，读取寄存器堆的源操作数。
3. **执行阶段（EX）**：ALU完成运算、地址计算、分支判断等操作。
4. **访存阶段（MEM）**：load/store指令访问数据存储器，其他指令跳过。
5. **写回阶段（WB）**：将运算结果或访存结果写回寄存器堆。
> 不同指令执行的阶段数不同：R型指令走完5段，分支指令在EX阶段结束跳转，store指令无WB阶段。

---

## 6.4 流水线 CPU
### 6.4.1 流水线基本原理
将指令执行的多个阶段**硬件独立化**，多条指令重叠执行，同一时刻不同阶段处理不同指令，提升吞吐率。
- 经典5级流水线：IF → ID → EX → MEM → WB，每级对应一个时钟周期。
- 核心指标：
  - 吞吐率：单位时间完成的指令数
  - 加速比：非流水线耗时 / 流水线耗时
  - 理想状态：流水线满载后，每个周期完成1条指令，CPI=1。

### 6.4.2 流水线 CPU 数据通路
在多周期通路基础上，**每两级之间加入流水线寄存器**（IF/ID、ID/EX、EX/MEM、MEM/WB），保存每一级的中间结果、控制信号；各段可同时工作，实现指令重叠执行。

### 6.4.3 RISC 适合流水线技术
RISC架构天然适配流水线，原因：
1. 指令定长、格式规整，取指译码简单。
2. Load/Store架构，仅load/store访存，其余指令操作寄存器，阶段均衡。
3. 寻址方式简单，地址计算快，执行阶段耗时短。
> CISC变长指令、复杂寻址、指令长度差异大，流水线实现难度高。

### 6.4.4 结构冒险（资源冲突）
- 成因：多条指令同时竞争同一硬件资源，导致冲突。最典型：取指和访存同时访问同一个存储器。
- 解决方法：
  1. 资源重复：分离指令存储器（I-cache）和数据存储器（D-cache）
  2. 指令预取：提前取出指令缓冲
  3. 流水线停顿（插入气泡）：冲突时暂停后序指令，等待资源释放

### 6.4.5 数据冒险
- 成因：指令间存在数据依赖，后序指令需要前序指令的结果，但结果尚未产生。
- 三类数据相关：
  1. **写后读（RAW）**：真数据相关，前写后读，最常见。
  2. **读后写（WAR）**：反相关，读操作被后序写操作覆盖。
  3. **写后写（WAW）**：输出相关，两次写的顺序错误导致结果错误。

### 6.4.6 数据旁路（转发/直通）技术
- 原理：将前序指令刚计算出的结果，直接从流水线寄存器转发到后序指令的ALU输入端，无需等待写回寄存器堆。
- 作用：解决绝大多数RAW数据冒险，避免流水线停顿，仅增加少量组合逻辑开销。

### 6.4.7 load-use 数据冒险
- 成因：load指令的结果在MEM阶段结束才产生，紧跟其后的指令在EX阶段就需要该结果，旁路也无法弥补1个周期的时间差。
- 现象：必须插入**1个时钟周期的停顿（气泡）**。
- 是流水线中无法单纯靠硬件旁路解决的典型数据冒险。

### 6.4.8 编译器重排指令顺序
- 原理：编译器调整指令顺序，将与load结果无关的指令插入到load和使用指令之间，填满停顿周期。
- 作用：在不改变程序逻辑的前提下，消除load-use气泡，提升流水线效率。

### 6.4.9 控制冒险
- 成因：分支/跳转指令需要等到执行阶段才能确定是否跳转、目标地址是多少，此时流水线已经预取了错误的后续指令。
- 解决方法：
  1. **静态分支预测**：默认预测不跳转，预测错误时冲刷流水线，清空错误指令。
  2. **动态分支预测**：硬件记录分支历史，动态预测跳转方向，提高预测准确率。
  3. **分支延迟槽**：编译器在分支指令后安排一条必然执行的指令，利用延迟周期。
  4. 提前分支计算：将分支判断和地址计算提前到译码阶段，减少停顿周期。

### 6.4.10 指令级并行（ILP）
在流水线基础上，进一步挖掘指令间的并行性，提升IPC（每周期指令数）：
- 多发射/超标量：一个时钟周期发射多条指令，配备多套执行部件。
- 超流水线：将流水线阶段拆得更细，提升主频。
- 乱序执行：动态调度指令执行顺序，避开依赖。
- 寄存器重命名：解决WAR、WAW伪相关，消除对应冒险。
- 重排序缓存（ROB）：保证指令提交顺序与程序顺序一致，实现精确异常。

# 第七章 输入输出系统

## 7.1 I/O 系统概述
I/O系统负责实现CPU与外部设备的数据交互，核心解决**CPU与外设速度不匹配、时序不匹配、数据格式不匹配**三大矛盾。
- I/O设备分类：输入设备、输出设备、存储设备、通信设备等。
- I/O系统层次：外设 → I/O接口 → 系统总线 → I/O控制模块，通过分层屏蔽底层硬件差异。

---

## 7.2 硬盘（外存设备）
### 7.2.1 磁盘驱动器
- 物理结构：盘片、磁头、主轴、磁臂；数据记录在盘片的磁道上，磁道划分为若干扇区，所有盘面同半径磁道组成柱面。
- 磁盘地址格式：**柱面号 → 盘面号 → 扇区号**，寻址时先定位柱面，再选磁头，最后等待扇区旋转到位。
- 读写原理：通过磁头的电磁效应，在磁介质上完成数据的读写。

### 7.2.2 磁盘的性能指标
- **存储容量：总容量 = 柱面数 × 盘面数 × 每磁道扇区数 × 每扇区字节数。**
- **平均存取时间 = 平均寻道时间 + 平均旋转延迟 + 数据传输时间**
  - 平均寻道时间：磁头移动到目标磁道的平均耗时。
  - **平均旋转延迟：盘片旋转半圈的时间，公式为 `60/(2×转速)`。**
- **数据传输率：单位时间传输的数据量 = 每磁道容量 × 磁盘转速。**

### 7.2.3 主机和磁盘间的数据交换
- 磁盘控制器作用：**串并转换、数据缓冲、地址译码、错误校验、控制磁盘机械动作**。
- 传输过程：CPU下发读写命令 → 磁盘控制器驱动磁盘执行操作 → 数据经控制器缓冲后，完成主存与磁盘的交互。

### 7.2.4 固态硬盘（SSD）
- 原理：基于闪存芯片存储数据，无机械运动部件。
- 特点：随机读写速度快、延迟低、抗震性强、功耗低；缺点是有擦写寿命限制，单位容量价格高于机械硬盘。
- 适用场景：系统盘、高频随机读写场景。

### 7.2.5 磁盘阵列（RAID）
核心思想：**多盘并行提升读写性能，冗余校验提升数据可靠性**。
常见级别核心对比：

| RAID级别 | 核心方案                           | 冗余能力        | 磁盘利用率 | 性能特点                           |
| -------- | ---------------------------------- | --------------- | ---------- | ---------------------------------- |
| RAID 0   | 数据条带化，多盘并行读写           | 无              | 100%       | 读写性能最高，无容错能力           |
| RAID 1   | 磁盘镜像，两两数据完全相同         | 强              | 50%        | 读性能提升，写性能无增益，可靠性高 |
| RAID 5   | 分布式奇偶校验，校验信息分散在各盘 | 可容忍1块盘损坏 | (n-1)/n    | 读写均衡，无单独校验盘瓶颈         |
| RAID 10  | 先做镜像再做条带（RAID1+RAID0）    | 强              | 50%        | 性能与可靠性兼顾，硬件成本高       |

---

## 7.3 I/O 接口
### 7.3.1 I/O 接口的通用结构
- **核心功能：地址译码、数据缓冲（匹配速度差）、电平/数据格式转换、提供状态与控制通道、错误检测。**
- 三类可编程端口：
  - **数据端口**：存放待传输的数据，可读可写。
  - **状态端口**：存放设备状态（就绪、忙、错误等），只读。
  - **控制端口**：存放CPU下发的控制命令，只写。

### 7.3.2 I/O 端口及其编址
#### 1. 统一编址（存储器映射I/O）
I/O端口与主存**共用同一地址空间**，使用普通访存指令操作I/O。
- 优点：指令丰富，寻址灵活，无需专用I/O指令。
- 缺点：占用主存地址空间，地址译码范围大。

#### 2. 独立编址（I/O映射I/O）
I/O端口拥有**独立地址空间，使用专用的`IN/OUT`指令访问。**
- 优点：不占用主存空间，地址线少，译码简单。
- 缺点：指令功能单一，寻址方式少。

---

## 7.4 I/O 控制方式
### 7.4.1 程序直接控制方式（程序查询方式）
- 原理：CPU循环读取设备状态寄存器，直到设备就绪才执行数据传输。
- 流程：发启动命令 → 轮询状态 → 就绪则传输 → 未就绪继续等待。
- 特点：硬件结构简单；**CPU全程参与**，利用率极低，CPU与设备串行工作。
- 适用：低速、简单外设，对效率要求极低的场景。

### 7.4.2 中断控制 I/O 方式
- 原理：设备就绪后主动**向CPU发送中断请求；CPU暂停当前程序，转入中断服务程序完成数据传输，结束后返回原程序**。
- 优势：CPU与设备可并行工作，CPU利用率显著高于查询方式。
- 不足：每次传输都需要**中断响应、现场保护与恢复，中断开销较大**。

### 7.4.3 中断响应和中断处理流程
完整执行流程：
1. **中断请求**：外设向CPU发送中断请求信号。
2. **中断判优**：多中断同时请求时，按优先级排序响应。
3. **中断响应**：满足条件（开中断、有请求、当前指令执行完毕）后，执行**关中断、保存断点、取中断向量**三个动作。
4. **中断服务**：保护现场 → 执行I/O传输等服务逻辑 → 恢复现场。
5. **中断返回**：开中断、恢复断点，回到原程序断点处继续执行。

### 7.4.4 多重中断（中断嵌套）
- 概念：高优先级中断可以打断正在执行的低优先级中断服务程序。
- 实现条件：中断服务程序中提前开中断；硬件支持优先级判优机制。
- **中断屏蔽：可通过中断屏蔽字动态调整中断的响应优先级。**

### 7.4.5 中断方式适用设备
中低速、单次传输数据量小的设备，如键盘、串口、打印机、普通外设。

### 7.4.6 DMA 控制方式
- 原理：由**DMA控制器（DMAC）**接管总线控制权，直接完成外设与主存之间的批量数据传输，传输全程无需CPU干预。
- 工作三阶段：
  1. 预处理：CPU向DMAC配置传输参数（内存起始地址、传输长度、传输方向）。
  2. 数据传输：DMAC控制总线，逐字/逐块完成传输，CPU不参与。
  3. 后处理：传输结束，DMAC发中断通知CPU，CPU做收尾校验。
- **总线传送方式：停止CPU访存、周期挪用（周期窃取）、交替访存。**

### 7.4.7 DMA 方式适用设备
高速、批量数据传输的设备，如磁盘、高速网卡、显卡、高速数据采集设备。

> 三种方式核心对比：CPU参与度 程序查询 > 中断 > DMA；传输效率 DMA > 中断 > 程序查询。

---

## 7.5 外部中断和内部异常
### 7.5.1 外部中断
- 来源：CPU外部的**I/O设备、外部事件触发。**
- 特点：**异步**，与当前执行的指令无关；分为可屏蔽中断和不可屏蔽中断。

### 7.5.2 内部异常
- 来源：CPU执行**指令过程中检测到的异常事件，如除零错误、非法指令、地址越界、缺页、系统调用。**
- 特点：**同步**，与当前指令直接相关，指令执行时触发。
- 分类：
  - **故障：可恢复异常**，处理完成后重新执行出错指令。
  - **陷阱：主动触发（如系统调用），异常后执行下一条指令。**
  - **终止**：严重错误，程序无法继续运行，直接终止。

---

## 7.6 异常和中断识别方式
### 7.6.1 软件识别方式
- 原理：CPU响应中断后，运行统一的查询程序，逐个检测中断源状态，找到触发源再跳转对应处理程序。
- 特点：硬件简单，响应速度慢，优先级由软件查询顺序决定。

### 7.6.2 硬件识别方式（向量中断）
- 原理：每个中断源对应唯一的中断向量，由硬件直接提供中断服务程序的入口地址。
- 核心机制：中断向量表，集中存储所有中断服务程序的入口地址。
- 特点：响应速度快，硬件开销稍大，是主流实现方式。

---

## 7.7 ISA 设计与异常中断
异常和中断机制是ISA的必要组成部分，ISA需要明确规定：
- 异常/中断的类型、编码、优先级规则。
- 中断响应的时序、栈帧格式、状态切换规则。
- 中断向量表的格式、存储位置。
保证硬件实现与软件处理遵循统一规范，是软硬件接口的重要部分。

------



# 第一章 导论

## 1.1 操作系统的功能

### 1.1.1 什么是操作系统

操作系统是配置在计算机硬件之上的第一层系统软件，是对硬件系统的首次扩充，负责管理软硬件资源，是用户与计算机硬件之间的接口。

### 1.1.2 操作系统的作用

三大核心作用：

1. **用户接口**：屏蔽硬件细节，通过命令、图形界面、系统调用等方式为用户提供易用的操作入口。
2. **资源管理者**：管理CPU、内存、I/O设备、文件四类核心资源，完成资源的分配、调度与回收。
3. **扩展机器（虚拟机）**：对硬件能力进行抽象和扩充，向上提供更易用的逻辑资源（如虚拟内存、文件系统）。

### 1.1.3 关键抽象概念

操作系统通过四大核心抽象屏蔽硬件复杂性：

- **进程/线程**：对CPU运行状态的抽象。
- **虚拟内存**：对物理内存的抽象，为每个进程提供独立地址空间。
- **文件**：对磁盘存储与I/O设备的抽象，提供统一的读写接口。
- **设备**：对各类外设的标准化抽象，屏蔽硬件差异。

---

## 1.2 操作系统的安装和启动

### 1.2.1 操作系统的安装

核心是将操作系统内核、系统文件部署到存储设备，完成磁盘分区、文件系统创建、引导程序写入、系统文件复制、硬件配置初始化等步骤。

### 1.2.2 操作系统的启动（引导过程）

典型启动流程：

1. **加电自检（POST）**：BIOS/UEFI初始化硬件，完成硬件状态检测。
2. **引导加载**：读取磁盘引导扇区（MBR/GPT），加载引导程序（如GRUB）到内存。
3. **内核加载**：引导程序将操作系统内核加载到内存并完成解压。
4. **控制权移交**：CPU从引导程序转交到内核，操作系统开始运行。

### 1.2.3 操作系统的初始化

内核启动后依次完成：

1. 内核核心数据结构初始化（进程表、内存管理表、驱动表等）。
2. 加载设备驱动，初始化外设。
3. 启动系统守护进程，初始化用户环境，最终呈现登录界面或Shell。

---

## 1.3 操作系统的发展 - 批处理系统

### 1.3.1 什么是作业

作业是用户提交给系统的一个完整计算任务，包含程序、数据、作业控制说明，是批处理系统的基本调度单位。

### 1.3.2 单道批处理系统

- 特点：内存中始终只有一道作业，作业成批输入、串行顺序执行。
- 进步：解决了人工操作的低效问题，减少了机器等待时间。
- 局限：CPU利用率低，I/O操作时CPU空闲；内存利用率低。

### 1.3.3 多道批处理系统

- 核心思想：内存中同时存放多道作业，当一道程序因I/O阻塞时，CPU切换到另一道程序运行。
- 优势：大幅提升CPU利用率、内存利用率和系统吞吐量。
- 局限：作业平均周转时间长；无交互能力，用户提交后无法干预。
- 地位：现代操作系统并发技术的雏形。

---

## 1.4 操作系统的发展 - 分时和实时系统

### 1.4.1 分时系统

- 产生背景：解决批处理系统无交互能力的问题，支持多用户同时联机使用。
- 核心机制：**时间片轮转**，CPU划分为细小的时间片，轮流为每个终端用户服务。
- 四大特征：多路性、独立性、及时性、交互性。
- 核心指标：响应时间。

### 1.4.2 实时系统

- 核心要求：任务必须在**规定截止时间**内完成，正确性同时取决于结果正确和时间合规。
- 分类：
  1. **硬实时系统**：截止时间绝对严格，超时会造成严重后果（工业控制、航天系统）。
  2. **软实时系统**：允许偶尔超时，后果可接受（视频播放、流媒体）。
- 特点：高可靠性、低延迟、强优先级调度。

---

## 1.5 操作系统的特征

四大基本特征，**并发是最核心的特征**，其余特征均基于并发产生。

### 1.5.1 并发（Concurrence）

- 概念：宏观上多个程序在一段时间内同时运行；单CPU下微观上交替执行，多CPU下可实现真正并行。
- 辨析：并发≠并行，并发是“宏观同时、微观交替”，并行是“物理上真正同时执行”。
- 实现载体：进程与线程。

### 1.5.2 共享（Sharing）

- 概念：系统资源可供内存中多个并发进程共同使用。
- 两种模式：
  1. **互斥共享**：同一时刻仅允许一个进程使用（如打印机、临界资源）。
  2. **同时共享**：宏观上同时访问，微观上交替访问（如磁盘、可重入代码）。

### 1.5.3 虚拟（Virtual）

- 概念：通过时分复用、空分复用技术，将一个物理实体虚拟为多个逻辑实体，让用户感知到更多资源。
- 典型应用：虚拟处理器、虚拟内存、虚拟设备（SPOOLing）。

### 1.5.4 异步（Asynchronism）

- 概念：进程以不可预知的速度向前推进，执行顺序、完成时间不确定；但只要运行环境相同，最终结果唯一确定。
- 成因：进程并发执行、资源竞争导致执行“走走停停”。
- 是进程同步互斥问题的根源。

---

## 1.6 操作系统是如何执行的

操作系统是**中断驱动**的，自身没有主动运行的主程序：

1. 常态下CPU运行用户态的用户程序。
2. 发生中断、异常或系统调用时，CPU陷入内核态，操作系统接管并处理事件。
3. 处理完成后返回用户态，继续执行用户程序。

---

## 1.7 CPU 的双重工作模式

### 1.7.1 内核态和用户态

为保护系统安全，CPU划分两个特权级：

- **内核态（管态）**：最高特权，可执行全部指令，访问所有硬件与内存资源，操作系统内核运行在此模式。
- **用户态（目态）**：低特权，仅能执行非特权指令，访问受限资源，用户程序运行在此模式。
- 状态切换：用户态→内核态仅能通过**中断/异常/系统调用（陷入）**；内核态→用户态通过中断返回、修改程序状态字实现。

### 1.7.2 特权指令 vs 非特权指令

- **特权指令**：仅内核态可执行，如I/O指令、关中断、内存管理指令等；用户态执行会触发异常。
- **非特权指令**：用户态可安全执行，如普通算术运算、受限访存指令。

---

## 1.8 系统调用的实现

### 1.8.1 硬中断和软中断

- **硬中断**：外部硬件触发，异步发生，如磁盘完成中断、时钟中断、键盘中断。
- **软中断**：CPU执行指令主动触发，同步发生，如系统调用（陷入指令）、缺页异常、除零异常。

> 系统调用本质是软中断，通过陷入指令主动完成用户态到内核态的切换。

### 1.8.2 系统调用

- 定义：操作系统内核提供给用户程序的功能接口，是用户程序请求内核服务的**唯一合法方式**。
- 执行流程：
  1. 用户程序调用库函数，将系统调用号、参数存入指定寄存器。
  2. 执行陷入指令（trap/int），触发软中断，CPU切换到内核态。
  3. 内核根据系统调用号查询系统调用表，执行对应内核服务函数。
  4. 处理完成后写入返回值，中断返回，切回用户态继续执行。

---

## 1.9 系统调用的类型

### 1.9.1 系统调用 vs 过程调用

| 对比维度   | 系统调用                   | 普通过程调用       |
| ---------- | -------------------------- | ------------------ |
| 特权级变化 | 用户态↔内核态切换          | 始终在用户态       |
| 执行开销   | 大，包含状态切换、中断处理 | 小，直接函数跳转   |
| 安全性     | 内核管控，访问受控         | 进程内执行，无保护 |

### 1.9.2 系统调用的类型

按功能分为六大类：

1. **进程控制类**：创建/终止进程、进程调度、等待、信号操作。
2. **文件操作类**：文件打开/关闭/读写、目录管理、文件创建删除。
3. **设备管理类**：设备申请/释放、设备读写、设备控制。
4. **内存管理类**：内存分配/释放、虚拟内存操作。
5. **信息维护类**：获取系统时间、进程状态、系统参数。
6. **通信类**：进程间通信（管道、消息、共享内存等）。

---

## 1.10 中断和异常总结

### 1.10.1 中断和异常的分类

按来源与性质划分：

1. **中断（外中断）**：CPU外部触发，异步
   - 可屏蔽中断：I/O设备中断，可通过屏蔽位关闭。
   - 不可屏蔽中断：电源故障、内存校验错误等，必须响应。
2. **异常（内中断）**：CPU内部触发，执行指令时同步发生
   - 故障：可恢复，处理完成后重新执行出错指令（如缺页异常）。
   - 陷阱：主动触发，处理完成后执行下一条指令（如系统调用）。
   - 终止：严重错误，程序无法继续运行（如非法指令、硬件故障）。

### 1.10.2 X86-64 系统的中断和异常

- 共256个中断向量：0~31为系统异常（如0号除零异常、14号缺页异常），32~255为外部中断。
- 通过**中断描述符表（IDT）**存储每个中断/异常的处理程序入口地址。
- 硬件自动完成中断响应：保存现场、切换内核栈、查表跳转；处理完成后通过iret指令恢复现场并返回。



# 第二章 文件系统

## 一、文件的基本概念

### 1.1 文件是对I/O设备的抽象

文件是操作系统对I/O设备的核心抽象，磁盘、键盘、显示器、网络等所有I/O设备都可被视为文件。应用程序通过统一的`open/read/write/close`系统调用完成各类I/O操作，无需关心底层硬件的实现细节。

- 磁盘文件：通过路径访问持久化存储的数据
- 字符设备：键盘、显示器以文件形式提供读写接口
- 网络：套接字也被抽象为文件，支持统一的读写操作

### 1.2 文件的两类数据

每个文件包含两类数据，均持久化存储在磁盘等外存中，断电不丢失：

1. **文件内容数据**：文件承载的实际业务数据，是字节序列的集合。
2. **文件元数据**：描述文件的属性信息，包括文件名、文件类型、大小、权限、所有者、创建/修改时间、物理块地址等，存储在文件控制块（FCB / inode）中。

------

## 二、磁盘分区与文件系统基础

### 2.1 磁盘分区

大容量磁盘会划分为多个独立分区，核心目的是**降低单个文件系统管理的盘块数量**，提升管理性能，减少内存开销。

- 每个分区可独立创建不同类型的文件系统
- 磁盘0号扇区为主引导记录（MBR），包含分区表与系统引导程序

### 2.2 文件系统的两大核心问题

设计文件系统必须解决两个核心问题：

1. **空闲盘块管理**：如何追踪、分配、回收磁盘中的空闲存储块
2. **文件盘块组织**：如何将一个文件的多个数据块组织存储在外存中

------

## 三、空闲盘块管理

共五种主流管理方案，各适用于不同场景：

### 3.1 空闲表法

- 原理：用表格记录所有连续空闲区，每个表项包含「起始盘块号 + 空闲盘块数」。
- 碎片概念：
  - **外部碎片**：夹在已分配区间之间、过小难以利用的空闲块
  - **内部碎片**：已分配块内部未被文件使用的剩余空间
- 三种分配算法：
  - **首次适应（FF）**：从表头开始查找第一个满足大小的空闲区，分割分配。优点是查找快，保留高地址大空闲区；缺点是低地址区域易产生大量外部碎片。
  - **最佳适应（BF）**：遍历所有空闲区，选择容量最小且满足需求的分区。优点是尽量保留大空闲区；缺点是易产生大量难以利用的小碎片，且需遍历全表，查找效率低。
  - **最坏适应（WF）**：选择最大的空闲区分割分配。优点是剩余分区仍较大，可被再次利用，外部碎片少；缺点是大分区被优先消耗，后续超大文件可能无法分配。
- 特点：适合连续分配方式；基于数组实现，回收合并空闲区时需移动表项，开销较大。

### 3.2 空闲链表法

将空闲块/空闲区通过指针串联成链表，避免表项移动开销。

#### 按节点粒度分类

- **空闲盘块链**：以单个盘块为链表节点，分配时从表头逐个取块，回收时挂到表尾。优点是单块分配回收操作简单；缺点是多块分配需多次操作，链表整体很长。
- **空闲盘区链**：以连续空闲区为链表节点，每个节点包含起始块号、块数、前后指针。支持首次/最佳/最坏适应算法，回收时需合并相邻空闲区。链表更短，多块分配效率更高。

#### 按链接方式分类

- **显式链接**：在内存中维护链表结构（节点含块号+指针）。分配回收无需读磁盘，速度快；占用额外内存空间。
- **隐式链接**：指针直接存放在空闲盘块内部，内存仅保存头尾指针。不占用额外内存；但分配回收需读写磁盘块，性能较低，且每个盘块需预留指针空间，减少了可用存储容量。

### 3.3 位示图法

- 原理：用二进制位矩阵映射每个盘块，通常`0`表示空闲，`1`表示已分配（定义可互换）。
- 核心计算（盘块号从0开始，每行n位）：
  - **已知行号i、列号j → 盘块号 `b = i × n + j`**
  - **已知盘块号b → 行号 `i = b // n`，列号 `j = b % n`**
- 分配：顺序扫描找到0位，置1并计算对应盘块号；支持查找连续n个空闲块。
- 回收：根据盘块号计算行列位置，将对应位清0即可。
- 特点：体积小，可常驻内存；分配回收速度快，是最常用的空闲管理方案之一。

### 3.4 成组链接法

- 定位：空闲表法与空闲链表法的结合方案，UNIX/Linux经典实现，专为大容量磁盘设计，避免表/链过长问题。
- 原理：将空闲盘块按固定大小分组；每组的所有块号记录在上一组的最后一个盘块中；最后一组的块号信息保存在超级块中，系统启动时加载到内存，作为空闲盘块号栈使用。最后一组的0号位置存放结束标记（如0），标识链尾。
- 分配：从内存栈顶依次出栈分配；当前组分配完毕时，将下一组的第一个盘块内容读入内存栈，继续分配。
- 回收：回收块号压入内存栈；栈满时，将栈中全部块号写入新回收的盘块，重置栈指向新组。
- 特点：组内顺序分配（O(1)复杂度），组间链式管理；兼顾性能与空间开销，无超长表/链问题。

### 3.5 FAT表法

- **原理：文件分配表（FAT）是磁盘上的全局数组**，下标对应盘块号，元素值表示下一块的块号。
- 特殊标记：通常用特定值区分空闲块、文件结束块、坏块。例如FAT32中，0表示空闲，`0x0FFFFFFF`表示文件尾，`0x0FFFFFF7`表示坏块。
- 双重作用：
  1. **空闲管理**：通过空闲标记识别未分配块，遍历FAT即可完成分配与回收。
  2. **文件组织**：串联单个文件的所有数据块，实现显式链接分配。
- 特点：空闲管理与文件分配共用同一张表，结构紧凑；但大分区下FAT体积大，全量加载占用内存高，适合中小容量存储。

------

## 四、文件盘块组织（文件物理结构）

文件物理结构指文件数据在外存上的存储组织形式，共三种主流方式。

### 4.1 连续分配

- 原理：为每个文件分配一段连续的磁盘块，FCB中记录「起始盘块号 + 占用块数」。
- 优点：
  - 顺序访问速度最快，无需额外寻道
  - 支持随机访问，通过偏移量可直接计算物理块号
  - 实现逻辑简单
- 缺点：
  - 随文件增删产生大量外部碎片，磁盘利用率低
  - 文件动态扩展困难，需提前预知文件大小
- 适用场景：文件大小固定、极少修改的场景。

### 4.2 链接分配

- 原理：为文件分配离散的磁盘块，通过指针串联成链表。

#### 隐式链接

每个数据块内部存放下一块的指针，FCB记录文件的首尾块号。

- 优点：无外部碎片，文件扩展灵活
- 缺点：只能顺序访问，随机访问效率极低；指针占用块内存储空间（如1KB块中4B存指针，仅1020B存数据）

#### 显式链接（FAT）

所有块的指针集中存放在FAT表中，内存查表即可遍历文件块链。

- 优点：随机访问性能优于隐式链接（查表在内存完成，仅最后一步读磁盘）；无外部碎片
- 缺点：FAT表占用内存空间大
- 代表：FAT12/16/32系列文件系统

### 4.3 索引分配

为每个文件分配独立的索引块，集中存放该文件所有数据块的地址。

#### 直接索引

FCB中直接存放数据块号，访问时无需额外读索引块。小文件性能极高；但文件越大，FCB中地址项越多，占用空间越大。

#### 间接索引

索引块中存放数据块号，FCB仅记录索引块地址。

- 一级间接：索引块 → 数据块；访问数据需1次额外IO
- 二级间接：一级索引块 → 二级索引块 → 数据块；访问数据需2次额外IO
- 三级间接：三层索引嵌套；访问数据需3次额外IO

#### 混合索引（增量式索引）

现代文件系统主流方案，FCB中同时包含直接块、一级间接、二级间接、三级间接地址，兼顾小文件速度与大文件容量。

- 核心计算（盘块大小4KB，块号占4B）：
  - 一个索引块可存放 `4KB / 4B = 1024` 个块号
  - 若有10个直接块：小文件最大 `10 × 4KB = 40KB`，访问数据0次额外IO
  - 加1个一级间接：最大支持 `1024 × 4KB = 4MB`，访问数据1次额外IO
  - 加1个二级间接：最大支持 `1024 × 1024 × 4KB = 4GB`，访问数据2次额外IO
  - 加1个三级间接：最大支持 `1024³ × 4KB = 4TB`，访问数据3次额外IO
- 优点：小文件访问快，大文件支持能力强；支持随机访问；无外部碎片
- 缺点：索引块占用额外空间，大文件多级索引增加IO次数

### 4.4 FCB与索引结点（inode）

- FCB（文件控制块）：通用概念，存储文件全部元数据与数据块地址，与文件一一对应。
- inode（索引结点）：索引分配下的FCB，是Linux/UNIX中的具体实现，每个inode有唯一编号，存储文件元数据与块指针。
- **FCB分解**：将目录项拆分为「文件名 + inode号」，其余元数据全部存入inode。大幅缩小目录项体积，减少目录检索的磁盘IO次数，显著提升查找性能。

### 4.5 三种分配方式对比

| 特性     | 连续分配       | 链接分配            | 索引分配       |
| -------- | -------------- | ------------------- | -------------- |
| 随机访问 | 支持，速度最快 | 隐式不支持，FAT支持 | 支持，性能较好 |
| 顺序访问 | 最快           | 较快                | 一般           |
| 外部碎片 | 严重           | 无                  | 无             |
| 扩展能力 | 差             | 好                  | 好             |
| 额外开销 | 无             | 指针开销            | 索引块开销     |

------

## 五、文件的逻辑结构

- **物理结构**：文件在外存的存储组织形式，用户不可见，由操作系统管理。
- **逻辑结构**：用户视角下的文件组织形式，分为有结构与无结构两类。

### 5.1 有结构文件（记录式文件）

由若干条记录组成，按记录是否等长分为：

- **定长记录**：所有记录长度相同，可随机访问，查找效率高；常用于数据库、业务数据文件。
- **变长记录**：记录长度不固定，每条记录前置长度字段；只能顺序访问，修改开销大；适合评论、日志等长度差异大且少修改的场景。

### 5.2 无结构文件（流式文件）

- 由连续的字节流组成，无内部记录结构。
- 灵活性最强，源程序、可执行文件、文本文件均属此类。
- 内核视角：操作系统将所有文件都视为字节流，逻辑结构由上层应用程序解释。

------

## 六、文件目录

### 6.1 目录的作用

目录是FCB的有序集合，核心目标是实现**按名存取**，同时满足：

1. 提升目录检索速度
2. 允许文件重名
3. 支持文件共享

### 6.2 目录结构演进

#### 单级目录

整个系统只有一张目录表。结构最简单；但查找慢、不允许重名、不便于共享，仅适用于单用户系统。

#### 两级目录

主目录 + 各用户独立子目录。解决了多用户间重名问题；但用户内部仍受限，层级不灵活。

#### 树形目录

多级层次结构，现代操作系统主流方案。

- 路径分为绝对路径（从根目录出发）和相对路径（从当前工作目录出发）
- 彻底解决重名问题，层次清晰，便于分类管理
- 目录本身也是文件（目录文件），有自己的inode，数据块中存放目录项

### 6.3 FCB分解对目录检索的优化

未分解时，目录项是完整FCB，体积大，一个盘块容纳的目录项少，检索时IO次数多。 分解后，**目录项仅含「文件名+inode号」**，体积大幅缩小，相同盘块可容纳更多目录项，平均检索IO次数显著减少；找到对应inode号后，再单独读取inode获取完整元数据。

------

## 七、简单文件系统 VSFS 实现

VSFS是简化版类Linux文件系统，采用「位图法管理空闲块 + 索引分配组织文件」。

### 7.1 VSFS 磁盘布局

从低地址到高地址依次为：

1. **超级块**：保存文件系统全局元数据（总块数、inode总数、各区域起始位置、文件系统类型），有多副本备份防损坏。
2. **inode位图**：标记inode的分配/空闲状态。
3. **数据位图**：标记数据块的分配/空闲状态。
4. **inode表**：集中存放所有inode，每个inode对应一个文件/目录；inode总数决定了文件系统支持的最大文件数。
5. **数据区**：存放普通文件数据与目录文件的目录项数据。

### 7.2 核心操作流程（无缓存）

- **创建目录**：分配空闲inode → 初始化目录inode → 父目录新增目录项 → 同步更新磁盘元数据
- **创建文件**：逐级路径解析找到父目录 → 分配新inode → 父目录新增目录项 → 同步磁盘
- **打开文件**：逐级路径解析 → 读取目标文件inode到内存
- **写文件**：分配数据块 → 更新inode块指针 → 写入数据 → 同步磁盘
- **删除文件**：路径解析找到inode → 删除对应目录项 → 释放数据块 → 释放inode

------

## 八、文件系统缓存与内核结构

通过内存缓存磁盘数据，大幅减少磁盘IO，提升文件操作性能。

### 8.1 四级缓存结构

1. **进程级打开文件表**：每个进程私有，记录文件描述符、独立读写指针、访问模式，指向系统级表项。
2. **系统级打开文件表**：全局唯一，记录打开文件的内存inode、引用计数；同一文件被多进程打开时共享同一项。
3. **目录项缓存（dentry cache）**：缓存「文件名 → inode号」的映射，省去读取目录数据块的IO。
4. **页缓存（Page Cache）**：缓存磁盘数据页，包括文件数据块、目录数据块，是最底层的块级缓存。

### 8.2 open 路径解析的IO分析

以`open("/a/b/c.txt")`为例，路径分为 `/`、`a`、`b`、`c.txt` 四级，每级需「目录inode + 目录数据块」两个信息，根据缓存命中情况IO次数不同：

#### 场景1：冷启动，全部缓存为空

每级目录需读inode + 读目录数据块，最后读目标文件inode

- 总IO：7次
- 说明：open仅读取元数据，不加载文件内容数据块

#### 场景2：仅dentry cache全部命中

dentry直接提供所有级别的inode号，无需读目录数据块；但inode不在内存

- 依次读根、a、b、c.txt共4个inode
- 总IO：4次
- 核心：dentry只存文件名与inode号的映射，不存inode内容，inode未缓存时仍需读磁盘

#### 场景3：dentry + inode cache全部命中

全程内存操作，无需访问磁盘

- 总IO：0次
- 典型场景：短时间重复打开同一文件，close不会清空内核缓存

#### 场景4：部分命中（仅根目录缓存命中）

根目录0次IO，下级每级需读inode + 目录数据块

- 总IO：5次

### 核心考点总结

| 缓存条件                      | open磁盘IO次数 | 说明                                              |
| ----------------------------- | -------------- | ------------------------------------------------- |
| 全部缓存为空（冷启动）        | 7              | 每级目录：inode + 目录数据块；最后加目标文件inode |
| dentry全部命中，inode缓存为空 | 4              | 跳过所有目录数据块，仅读4个inode                  |
| dentry + inode缓存全部命中    | 0              | 纯内存操作，无磁盘访问                            |

- dentry cache：消除目录数据块的IO
- inode cache：消除磁盘inode的IO
- open永远不读取文件内容，文件数据由read系统调用触发加载

### 8.3 其他文件操作

- **read/write**：先查页缓存，命中则直接内存拷贝；未命中则发起磁盘IO加载到缓存。
- **lseek**：仅修改进程打开文件表中的读写指针，纯内存操作，无IO。
- **close**：减少系统级表引用计数；计数归0时释放内存inode与缓存，脏数据写回磁盘。
- **remove**：根据路径删除目录项，释放数据块与inode；与文件是否被打开无关。

### 8.4 磁盘inode vs 内存inode

- **磁盘inode**：持久化存储在磁盘上，包含文件全部元数据与块指针。
- **内存inode**：文件打开时从磁盘加载到内存，除元数据外，额外包含：inode编号、状态标记、引用计数、所属设备号、哈希链表指针。

------

## 九、进程的工作目录

- 每个进程维护独立的当前工作目录（CWD），作为相对路径的解析起点。
- 相对路径比绝对路径检索层级少，速度更快。
- `.` 表示当前目录，`..` 表示上级目录。
- 核心作用：减少路径检索的目录层级，提升文件访问效率。

------

## 十、文件共享

### 10.1 硬链接

- **原理：多个目录项指向同一个inode，inode中维护链接计数。**
- 删除行为：**删除文件仅减少链接计数**；计数归0时才真正释放inode与数据块。
- 限制：
  1. 不能跨文件系统：不同文件系统inode号独立编号，无全局唯一性
  2. 不能链接目录：防止形成目录环，导致递归遍历程序死循环
- 优点：访问无额外开销，性能与原文件完全一致

### 10.2 符号链接（软链接）

- 原理：独立的链接文件，**有自己的inode，文件内容存储目标文件的路径字符串**。短路径可直接存在inode中，长路径需分配数据块存储。
- 特点：
  - 可跨文件系统，支持链接目录
  - 原文件删除后，软链接变为悬空链接，访问失败
  - 访问需额外解析路径，多一次IO开销
- 本质：类似Windows的快捷方式

### 10.3 硬链接 vs 软链接

| 维度       | 硬链接          | 软链接         |
| ---------- | --------------- | -------------- |
| inode      | 共享同一个inode | 拥有独立inode  |
| 跨文件系统 | 不支持          | 支持           |
| 目录链接   | 不支持          | 支持           |
| 删除原文件 | 仍可正常访问    | 链接失效       |
| 访问开销   | 几乎无          | 多一次路径解析 |

------

## 十一、文件权限

### 11.1 Linux UGO 权限模型

经典三级权限模型，用9个二进制位表示，对应三类身份、三类权限：

- **三类身份**：文件所有者（User）、所属用户组（Group）、其他用户（Other）
- **三类权限**：读（r）、写（w）、执行（x）
- 表示方式：
  - 符号表示：如 `rw-r--r--`
  - 八进制表示：如 `644`（所有者6=110，组4=100，其他4=100）

### 11.2 权限设置

- 文件创建时可通过`open`的mode参数指定初始权限，配合umask掩码过滤默认权限。
- 可通过系统调用动态修改文件权限与所有者。

### 11.3 其他权限模型

- **口令保护**：文件绑定访问口令，验证通过才可访问；仅控制能否访问，无法细分权限类型。
- **访问控制列表（ACL）**：为单个用户/组单独设置权限，比UGO模型更精细，适用于复杂权限场景。

------

## 十二、虚拟文件系统（VFS）

### 12.1 文件系统的多样性

磁盘可分区创建不同类型的文件系统，空闲管理与块组织方式各不相同（如ext4、XFS、FAT、NFS等），原生接口差异巨大。

### 12.2 VFS的作用

VFS是内核中的抽象适配层：

- 向上：对用户空间提供统一的`open/read/write`等系统调用接口
- 向下：定义标准操作接口，各具体文件系统实现该接口
- 核心价值：屏蔽底层文件系统差异，实现多文件系统透明共存，是Linux「一切皆文件」的基础。

### 12.3 VFS核心功能

- 统一管理系统级打开文件表、目录项缓存
- 通过vnode统一不同文件系统的inode编号，实现跨文件系统操作
- 负责文件系统的挂载管理

------

## 十三、Linux：一切皆文件

基于VFS，Linux将所有系统资源抽象为文件，通过统一文件接口访问。

### 13.1 设备文件系统（/dev）

- 将硬件设备抽象为文件，分为两类：
  - **字符设备**：按字节流顺序访问，如键盘、串口、/dev/null、/dev/urandom
  - **块设备**：按块随机访问，如磁盘
- inode中存储**主设备号 + 次设备号**：主设备号定位驱动程序，次设备号区分同类型的不同设备。
- 特殊设备：
  - `/dev/null`：黑洞设备，写入数据直接丢弃，读取返回空；用于屏蔽命令输出、清空文件
  - `/dev/urandom`：随机数设备，读取可生成随机字节流

### 13.2 特殊文件系统

- 无真实磁盘存储，内核运行时动态生成内容，常驻内存。
- 代表：
  - `/proc`：暴露进程与内核运行状态信息
  - `/sys`：暴露硬件设备与驱动信息
- 文件大小显示为0，读取时由内核实时生成内容。

### 13.3 七种文件类型

Linux中通过`ls -l`首字符区分：

- `-` 普通文件，`d` 目录文件
- `c` 字符设备，`b` 块设备
- `l` 符号链接，`s` 套接字，`p` 管道

------

## 十四、文件系统的挂载

### 14.1 挂载的概念

将一个独立文件系统接入Linux全局目录树的过程，挂载点是一个已存在的空目录。

- 访问挂载点路径 = 访问该文件系统的根目录
- 挂载点原有内容被临时隐藏，卸载后恢复

### 14.2 挂载表

内核维护挂载表，每条记录包含：挂载目录、文件系统类型、对应设备名。 路径解析时通过挂载表切换对应文件系统，实现多文件系统在单根目录树下的统一访问。

### 14.3 挂载的两个核心动作

1. **绑定**：将磁盘分区与挂载点目录关联
2. **加载元数据**：读取超级块、位图、inode表等文件系统元数据到内存，完成初始化



# 第三章 I/O设备管理

## 一、I/O系统概述

### 1.1 I/O系统的组成

I/O系统负责控制外设与内存、CPU之间的数据交换，由两部分构成：

- **I/O硬件**：外部设备、设备控制器、I/O控制部件等
- **I/O软件**：中断处理程序、驱动程序、设备独立性软件等

### 1.2 I/O系统的六大功能

1. **隐藏I/O设备细节** 通过`read/write/open/close`等统一系统调用屏蔽底层硬件操作差异，用户无需关心磁盘的磁道/扇区寻址、打印机的控制命令，即可操作设备。

2. **保证设备无关性** 应用程序使用逻辑设备名（如`/dev/printer`）访问设备，由系统运行时映射到具体物理设备。提升系统可移植性与扩展性，支持即插即用，设备更换无需修改应用代码。

   > 区别：隐藏细节解决「不同设备怎么用」的问题；设备无关性解决「同类设备用哪个」的问题，是更高层级的抽象。

3. **提高CPU与I/O设备利用率** 通过缓冲技术、中断驱动、DMA、I/O通道、磁盘调度等手段，让CPU与I/O设备并行工作，减少CPU空转。

4. **设备控制功能** 支持四种I/O控制方式：程序查询方式、中断方式、DMA方式、通道方式，适配不同速度的设备。

5. **设备共享管理**

- **独占设备**：如打印机、磁带机，同一时间仅分配给一个进程，需互斥访问
- **共享设备**：如磁盘，允许多个进程交叉访问，通过调度算法保证有序

6. **错误处理** 区分临时性错误（重试纠正）与持久性错误（向上层报告），尽可能在底层解决错误，高层仅处理无法修复的故障。

------

## 二、I/O系统的层次结构

I/O软件采用分层架构，自顶向下分为四层，每层利用下层服务、向上层提供接口：

### 2.1 四层结构

1. **用户层软件**：提供用户交互接口，用户通过库函数或系统调用发起I/O请求
2. **设备独立性软件（设备无关软件）**：实现统一接口、设备命名、设备保护、缓冲管理、设备分配与释放等通用功能，屏蔽底层设备差异
3. **设备驱动程序**：与硬件直接相关，将上层抽象请求转换为硬件可识别的具体命令，操作设备控制器完成I/O
4. **中断处理程序**：保存被中断进程的CPU现场，执行对应中断处理逻辑，处理完成后恢复现场并返回

### 2.2 三类设备接口

I/O系统按设备类型提供三类标准接口：

| 接口类型     | 对应设备             | 核心特点                           | 主要控制方式 |
| ------------ | -------------------- | ---------------------------------- | ------------ |
| 块设备接口   | 磁盘、固态硬盘、光盘 | 以数据块为单位传输、速率高、可寻址 | DMA方式      |
| 字符设备接口 | 键盘、打印机、串口   | 以字符为单位传输、速率低、不可寻址 | 中断驱动     |
| 网络接口     | 网卡                 | 面向网络通信，对接TCP/IP协议栈     | -            |

### 2.3 阻塞I/O vs 非阻塞I/O

- **阻塞I/O（BIO）**：进程发起I/O后被阻塞挂起，等待I/O完成后由中断唤醒，是系统默认的I/O方式
- **非阻塞I/O（NIO）**：发起I/O后立即返回，进程通过轮询检查数据是否就绪，效率较低；I/O多路复用可实现更高效的非阻塞I/O

------

## 三、设备驱动程序与I/O控制方式

### 3.1 驱动程序的执行流程

驱动程序完成一次I/O请求分为5个步骤：

1. **抽象转具体**：将上层的逻辑请求（如读取某盘块）转换为硬件可识别的参数（如磁头号、磁道号、扇区号），写入设备控制器寄存器
2. **请求校验**：检查I/O请求是否合法（如打印机不支持读操作），非法请求直接报错
3. **检查设备状态**：读取设备状态寄存器，确认设备就绪后才启动操作，否则等待
4. **传送参数**：向控制器寄存器写入数据地址、传输长度、工作模式等参数
5. **启动I/O设备**：下发控制命令启动设备，随后驱动程序阻塞，等待中断唤醒

### 3.2 四种I/O控制方式

#### （1）程序查询方式

CPU不断轮询设备状态寄存器，直到设备就绪才进行数据传输。

- 特点：CPU全程干预，利用率极低，I/O与CPU串行工作
- 适用：简单低速设备、嵌入式系统

#### （2）中断驱动方式

CPU启动设备后继续执行其他任务，设备完成I/O后向CPU发中断，CPU在中断处理中完成数据传输。

- 特点：CPU与I/O可并行，以字节/字为单位干预，每次传输都需中断
- 适用：低速字符设备（键盘、打印机）

#### （3）DMA方式

由DMA控制器接管总线，直接在设备与内存间批量传输数据，传输全程无需CPU参与，仅在开始和结束时CPU介入。

- 特点：CPU干预粒度从字节提升到数据块，大幅降低CPU占用
- 局限：一次只能传输一个连续数据块，多块传输需多次DMA操作

#### （4）通道控制方式

通道是专门的I/O处理器，可执行通道程序，一次能完成多个数据块的传输与控制。

- 通道指令包含操作码、内存地址、传输计数、结束标志等字段
- 特点：CPU干预粒度进一步降低到「一组数据块」，实现CPU与I/O的高度并行
- 适用：大型机、高并发I/O场景

### 3.3 驱动程序初始化

系统启动时驱动程序完成设备初始化，核心工作：

1. **检测设备状态，复位设备到初始状态**
2. 分配三类资源：I/O端口号/内存地址、内核缓冲区、中断号
3. **注册中断处理程序：申请中断号，将中断处理程序入口写入中断向量表**

### 3.4 中断处理程序与驱动的关系

中断处理程序是驱动程序的「下半部分」，与驱动深度绑定：

- 中断处理逻辑与硬件强相关，只能由对应驱动实现
- 中断向量由驱动注册，中断发生时直接跳转到驱动的处理函数
- 核心工作：读取设备状态、清除中断标志、唤醒阻塞进程、完成I/O收尾

------

## 四、设备独立性软件

设备独立性软件位于用户层与驱动层之间，核心目标是「上层不关心硬件细节，下层不关心用户逻辑」，包含五大核心功能：

### 4.1 五大核心功能

1. **映射**：将逻辑设备名转换为物理设备，找到对应驱动程序入口
2. **保护**：权限控制，限制用户直接操作硬件，所有设备访问必须经过系统调用
3. **分块**：适配上层数据与下层硬件的传输单位差异，将大数据拆分为固定大小的块
4. **缓冲**：管理内存缓冲区，缓和CPU与I/O速度差
5. **分配**：管理多进程对设备的竞争，按策略分配设备资源

### 4.2 设备分配机制

#### 核心数据结构

系统通过三张表实现设备、控制器、通道的三级管理：

1. **设备控制表（DCT）**：每个设备对应一项，记录设备状态、设备等待队列、指向控制器表的指针
2. **控制器控制表（COCT）**：每个控制器对应一项，记录控制器状态、控制器等待队列、指向通道表的指针
3. **通道控制表（CHCT）**：每个通道对应一项，记录通道状态、通道等待队列

补充：**系统设备表（SDT）**记录系统全部设备，包含设备类型、物理名、驱动入口、DCT指针，是逻辑设备到物理设备的查找入口。

#### 分配流程

进程请求设备时，依次检查设备、控制器、通道是否空闲，三者均空闲才分配成功；任意一级忙碌，则将进程PCB挂到对应等待队列。

### 4.3 设备名映射

#### 逻辑设备表（LUT）

维护逻辑设备名到物理设备的映射，应用程序仅使用逻辑名，设备更换或故障时系统自动重定向，无需修改代码。一个逻辑名可映射多个物理设备，提升容错与负载能力。

#### Linux主次设备号

Linux通过设备文件的inode存储主次设备号：

- **主设备号**：标识设备驱动类别，用于查找对应驱动程序入口
- **次设备号**：驱动内部使用，区分同类别下的具体物理设备

### 4.4 驱动程序管理

1. **统一接口管理**：定义标准化驱动接口（open/close/read/write/ioctl），所有硬件驱动必须实现，上层通过统一接口调用
2. **命名与映射管理**：维护逻辑设备与物理设备的映射关系
3. 辅助功能：驱动加载卸载、资源冲突管理、版本升级与校验

------

## 五、缓冲区管理

### 5.1 缓冲区基础

缓冲区是内存中的一块存储区域，核心作用是缓和CPU与I/O设备的速度不匹配。

#### 四大作用

1. 缓和CPU与I/O设备间速度不匹配的矛盾
2. 提高CPU与I/O设备的并行性
3. 减少对CPU的中断频率，放宽中断响应时间限制
4. 解决数据粒度不匹配问题

#### 缓冲 vs 缓存

- **缓冲**：保存数据的唯一现有版本，核心作用是平滑速度差、协调数据粒度

- 缓存

  ：保存数据的高速副本，核心作用是提升重复访问的速度

  > 同一块内存可同时兼具缓冲与缓存功能（如磁盘高速缓存）

### 5.2 单缓冲区

为每个I/O请求分配一个缓冲区，设备与CPU交替使用。 设T为设备传输一块数据的时间，M为缓冲区数据拷贝时间，C为CPU处理时间。

- **单块平均处理时间：`max(T, C) + M`**
- **连续n块总时间：**
  - **T > C时：`n × (T + M) + C`**
  - **T < C时：`T + n × (M + C)`**

### 5.3 双缓冲区

设置两个缓冲区，设备填充一个的同时CPU处理另一个，实现真正并行。

- **单块平均处理时间：`max(T, M + C)`**
- **连续n块总时间：**
  - **T > M+C时：`n × T + (M + C)`**
  - **T < M+C时：`T + n × (M + C)`**

> 双缓冲区可实现双向同时通信（独立的发送+接收缓冲区），单缓冲区只能单向串行通信。

### 5.4 环形缓冲区

多个缓冲区组成环形队列，分为空缓冲区R、满缓冲区G、工作缓冲区C三类，配合Nexti、Nextg、Current三个指针管理。

- **Getbuf**：获取缓冲区用于输入或计算
- **Releasebuf**：释放缓冲区，转换缓冲区类型
- 同步规则：输入速度 > 计算速度时输入进程阻塞（系统受计算限制）；计算速度 > 输入速度时计算进程阻塞（系统受I/O限制）

### 5.5 缓冲池

#### 设计目标

将专用缓冲区改为系统统一管理的公用缓冲池，动态分配给不同进程/设备，提升缓冲区利用率。

#### 组成

三个队列：

1. **空白缓冲队列emq**：空闲缓冲区组成的队列
2. **输入队列inq**：装满输入数据的缓冲区队列
3. **输出队列outq**：装满输出数据的缓冲区队列

四种工作缓冲区：

- 收容输入hin、提取输入sin
- 收容输出hout、提取输出sout

#### 工作流程

1. **收容输入**：从空白队列取空缓冲区，装入输入数据后挂入输入队列
2. **提取输入**：从输入队列取满缓冲区，提取数据后将空缓冲区放回空白队列
3. **收容输出**：从空白队列取空缓冲区，装入输出数据后挂入输出队列
4. **提取输出**：从输出队列取满缓冲区，输出数据后将空缓冲区放回空白队列

#### 同步互斥实现

每个队列配置1个互斥信号量MS（初始值1）、1个资源信号量RS（初始值为对应队列的缓冲区数量），通过`Getbuf`和`Putbuf`过程实现安全的队列操作。

------

## 六、磁盘I/O优化与调度

### 6.1 提升磁盘I/O速度的五种方法

1. **磁盘高速缓存**：内存中开辟区域缓存常用盘块副本，命中则无需访问磁盘；采用LRU置换策略，周期性回写脏数据块
2. **I/O调度算法**：优化磁盘请求处理顺序，减少磁头移动距离
3. **提前读+延迟写**：顺序访问时预读下一块数据；写操作暂存缓存，积累后批量写入磁盘
4. **优化物理块分布**：将同文件盘块分配在相邻磁道或同磁道，减少寻道与旋转延迟
5. **RAID技术**：多磁盘组成阵列，实现并行读写与冗余容错

### 6.2 磁盘调度算法

磁盘I/O的核心开销是寻道时间，调度算法通过优化请求顺序减少磁头移动。

#### （1）先来先服务（FCFS）

按请求到达顺序依次处理。

- 优点：公平简单，不会产生饥饿
- 缺点：平均寻道时间长，未做寻道优化
- 适用：请求数量少的场景

#### （2）最短寻道时间优先（SSTF）

每次选择距离当前磁头最近的磁道访问。

- 优点：平均寻道时间优于FCFS
- 缺点：可能产生饥饿（新请求持续出现在磁头附近，远端请求长期得不到服务）

#### （3）扫描算法（SCAN/电梯算法）

磁头沿一个方向移动，处理沿途所有请求，到达端点后反向移动。

- 优点：寻道性能好，彻底解决饥饿问题
- 特点：运行规律类似电梯，又称电梯调度算法

#### （4）循环扫描（C-SCAN）

磁头仅沿单一方向处理请求，到达最远端后直接跳转到另一端起点，继续同方向处理。

- 优点：各磁道等待时间更均匀，减少新请求的延迟波动

#### （5）LOOK与C-LOOK

SCAN/C-SCAN的优化版：磁头仅移动到当前方向最远的请求处即转向，无需走到磁盘物理边界。

#### （6）N步扫描（NStepSCAN）

将请求队列划分为长度为N的子队列，按FCFS顺序处理子队列，子队列内部用SCAN调度。

- 解决**磁臂粘着**问题：高频访问某磁道导致磁头停滞，其他请求无法处理
- 新请求放入后续子队列，不影响当前正在处理的队列

------

## 七、假脱机（Spooling）系统

### 7.1 基本概念

假脱机技术是在联机状态下模拟脱机输入输出，利用磁盘+多道程序设计，将独占设备虚拟为多台逻辑设备，实现独占设备的共享。

### 7.2 系统组成

1. 磁盘上的输入井+输出井
   - 输入井：模拟脱机输入的磁盘区域，收容低速设备输入的数据
   - 输出井：模拟脱机输出的磁盘区域，收容用户程序的输出数据
2. **内存中的输入缓冲区+输出缓冲区** 缓和磁盘与I/O设备的速度差，作为井与物理设备之间的数据中转。
3. **输入进程+输出进程** 模拟脱机时的外围控制机，分别负责输入设备→输入井、输出井→输出设备的数据传输。
4. **井管理程序** 控制用户进程与输入/输出井之间的数据交换。

### 7.3 假脱机打印机示例

打印机是典型独占设备，通过Spooling技术改造为共享设备：

1. 进程提交打印请求时，将打印数据写入输出井，生成打印请求加入等待队列
2. 进程无需等待打印完成，可继续执行其他任务
3. 打印机空闲时，打印进程从队列队首取请求，从输出井读取数据并打印
4. 宏观上多个进程同时使用打印机，每个进程感觉自己独占一台打印机

### 7.4 三大特点

1. **提高I/O速度**：将低速设备I/O转换为对磁盘的高速访问，缓和CPU与低速设备的速度矛盾
2. **独占设备改共享设备**：无需为进程分配物理设备，仅分配磁盘空间与请求表项
3. **实现虚拟设备功能**：宏观共享、微观独占，每个进程获得逻辑上的专属设备



# 第四章：内存管理（完整总结）

## 一、章节整体概述

本章是虚拟内存体系的基础篇，核心围绕两大问题展开：**一个进程占用多大内存**，以及**如何给进程分配内存**。

- 虚拟内存是操作系统对主存和外存的抽象，让应用程序无需关心数据的实际存储位置，统一通过虚拟地址访问。
- 本章聚焦内存管理的底层机制：从程序的编译链接、装入运行，到连续/离散内存分配方案，再到分页、分段、段页式三大管理模式，层层递进讲解内存的抽象与管理方法。

------

## 二、C程序的编译与链接

### 2.1 编译驱动与四阶段

C程序从源代码到可执行文件，由编译器驱动程序调度完成，分为**翻译阶段**和**链接阶段**，共4个步骤：

1. **预处理**：处理`#`开头的宏定义、头文件包含等指令，生成修改后的源程序
2. **编译**：将C代码翻译成汇编语言程序（`.s`文件）
3. **汇编**：将汇编代码翻译成机器指令，生成**可重定位目标文件**（`.o`文件）
4. **链接**：将多个可重定位目标文件合并，生成可执行目标文件

链接分为三类：

- 静态链接：运行前将所有目标文件和库函数合并为完整可执行文件
- 装入时动态链接：程序加载入内存时，边装入边完成链接
- 运行时动态链接：程序运行过程中按需加载目标模块并链接，节省内存空间

### 2.2 目标文件的三种形式

- 可重定位目标文件：链接器的输入，可与其他目标文件合并生成可执行文件
- 可执行目标文件：可直接加载到内存中运行
- 共享目标文件：特殊的可重定位文件，可在加载或运行时动态链接

现代x86-64 Linux/Unix系统采用**ELF（可执行可链接格式）**作为标准目标文件格式。

### 2.3 符号与符号表

每个程序模块包含符号定义和符号引用，链接器通过符号表管理所有符号：

- **全局符号**：非静态的全局函数、全局变量，可被其他模块引用
- **局部符号**：`static`修饰的函数、全局变量，仅本模块可见
- **本地符号**：函数内部定义的变量，由运行时栈管理，链接器不处理

符号表存储在`.symtab`节中，记录每个符号的名称、所属节、偏移地址、占用大小，是链接器完成符号解析和重定位的核心依据。

### 2.4 可重定位目标文件（ELF格式）

ELF可重定位文件由ELF头、多个节（section）、节头部表组成：

1. **ELF头**：描述文件的字长、字节序、文件类型、指令集、节头部表位置等元信息

2. 核心节区

   ：

   - `.text`：已编译的机器指令代码
   - `.data`：已初始化的全局变量和静态变量
   - `.bss`：未初始化/初始化为0的静态变量，不占用磁盘空间，仅作为占位符
   - `.rodata`：只读数据，如字符串常量
   - `.rela.text`/`.rela.data`：代码段/数据段的重定位条目，记录需要修改的符号引用位置
   - `.symtab`：符号表

3. **节头部表**：描述每个节在文件中的偏移位置、大小等属性，链接器通过它定位各个节。

### 2.5 链接的核心任务

链接器的核心工作是**符号解析**和**重定位**，分两步完成：

1. **符号解析**：将每个符号引用与唯一的符号定义关联起来，匹配不同模块间的外部符号

2. 重定位

   ：

   - 第一步：合并所有输入目标文件的同名节，为合并后的节、每个符号分配虚拟内存地址
   - 第二步：根据重定位条目，修改代码段和数据段中所有符号引用，使其指向正确的虚拟地址

重定位分为绝对寻址（直接写入符号的虚拟地址）和相对寻址（写入相对于PC的偏移量）两种方式。

### 2.6 可执行目标文件

链接完成后生成的可执行文件同样是ELF格式，包含段头部表，记录代码段、数据段与虚拟地址空间的映射关系：

- 代码段（`.init`、`.text`、`.rodata`）：只读可执行，映射到低地址的固定虚拟区间
- 数据段（`.data`、`.bss`）：可读可写，`.bss`节在磁盘不占空间，加载到内存时自动初始化为0
- 程序入口地址：记录在ELF头中，是程序运行时第一条指令的虚拟地址

------

## 三、程序的装入阶段

可执行文件需要通过加载器（装入程序）加载到内存才能运行，加载器工作在内核态，共有三种装入方式：

### 3.1 绝对装入方式

- 适用场景：单道程序系统，程序运行位置固定
- 特点：编译链接时直接生成绝对物理地址，加载器按地址直接将程序装入内存，无需修改
- 局限：仅适用于单任务系统，无法支持多道程序并发

### 3.2 可重定位装入方式（静态重定位）

- 适用场景：多道程序环境，程序起始逻辑地址从0开始
- 特点：加载时根据分配的内存起始地址，一次性将所有逻辑地址修改为物理地址
- 局限：程序装入后不能在内存中移动，否则地址会失效，无法支持内存换入换出

### 3.3 动态运行时装入方式（动态重定位）

- 核心机制：装入时不修改逻辑地址，地址转换推迟到指令真正执行时完成，依靠**重定位寄存器（基址寄存器）**硬件支持
- 地址转换：物理地址 = 逻辑地址 + 重定位寄存器中的基地址
- 优势：程序运行时可在内存中移动位置，仅需修改重定位寄存器的值即可，是现代操作系统的主流装入方式

------

## 四、进程的虚拟地址空间

### 4.1 地址空间概念

- **物理地址空间**：对应实际物理内存的地址集合，大小由内存硬件容量决定
- **虚拟地址空间（逻辑地址空间）**：进程视角的地址集合，是操作系统提供的抽象，每个进程拥有独立的虚拟地址空间
- 每个字节同时拥有虚拟地址和物理地址，CPU通过地址转换机制将虚拟地址映射为物理地址

### 4.2 进程虚拟地址空间布局

程序运行时，虚拟地址空间按功能分层布局（从低地址到高地址）：

1. 只读代码段：`.text`、`.rodata`，存放指令和只读数据
2. 读写数据段：`.data`、`.bss`，存放全局变量和静态变量
3. 运行时堆：通过`malloc`动态申请的内存，向高地址方向增长
4. 共享库映射区：存放动态链接库的代码和数据
5. 用户函数栈：函数调用的栈帧，向低地址方向增长
6. 内核空间：操作系统内核的代码和数据，所有进程共享

### 4.3 32位与64位差异

- 32位系统：总地址空间4GB，其中用户空间3GB（0x000000000xBFFFFFFF），内核空间1GB（0xC00000000xFFFFFFFF）
- 64位系统：仅使用低48位/57位虚拟地址，用户空间和内核空间分离，中间留有空洞隔离，最大寻址空间可达256TB/128PB

------

## 五、连续分配存储管理

连续分配指为进程分配一段连续的物理内存空间，分为多种实现方案。

### 5.1 单一连续分配

- 内存分为系统区和用户区，用户区仅运行一道程序，独占全部用户内存
- 实现简单，仅适用于单道程序系统

### 5.2 固定分区分配

- 将用户内存划分为若干个固定大小的分区，每个分区装入一道作业
- 内核通过**分区表**管理各分区的起始地址、大小和分配状态
- 特点：支持多道程序，但会产生**内部碎片**，内存利用率低

### 5.3 动态分区分配

根据进程实际需求动态分配内存，分区大小和数量随运行变化，内核通过**空闲分区表**或**空闲分区链**管理空闲内存。

#### 内存回收的四种情况

回收内存时需根据相邻空闲分区的情况，决定是否合并：

1. 仅与前一个空闲分区相邻：合并到前一个分区，修改前分区大小
2. 仅与后一个空闲分区相邻：合并后保留回收区起始地址，大小相加
3. 同时与前后两个分区相邻：三个分区合并为一个，取消后分区表项
4. 无相邻空闲分区：新增一个空闲分区表项

#### 基于顺序搜索的分配算法

| 算法         | 规则                                                   | 优点                             | 缺点                                 |
| ------------ | ------------------------------------------------------ | -------------------------------- | ------------------------------------ |
| 首次适应     | 空闲分区按地址升序排列，从链首查找第一个满足大小的分区 | 保留高地址大空闲区，分配速度较快 | 低地址产生大量外部碎片               |
| 循环首次适应 | 从上次分配的下一个分区开始查找                         | 空闲分区分布均匀，查找开销小     | 缺乏大空闲分区，外部碎片较多         |
| 最佳适应     | 空闲分区按容量升序排列，选择满足需求的最小分区         | 避免大材小用                     | 产生最多的外部碎片，释放时排序开销大 |
| 最坏适应     | 空闲分区按容量降序排列，选择最大的分区切割             | 产生碎片概率小，适合中小作业     | 缺乏大空闲分区，内存利用率偏低       |

#### 基于索引搜索的分配算法

适用于大型系统，按分区大小分类管理，提升查找速度：

1. **快速适应算法**：将空闲分区按大小分类，每类维护一个独立链表，通过索引表快速定位对应链表
2. **伙伴算法**：空闲分区大小均为2的幂次，按大小分级；分配时按需拆分大分区，回收时合并伙伴分区，Linux内核页帧管理采用此机制
3. **哈希算法**：以分区大小为关键字建立哈希表，实现快速查找

### 5.4 动态可重定位分区分配

#### 紧凑技术

当内存中零散空闲分区的总容量满足需求，但单个分区都不足时，通过移动内存中的进程，将所有空闲分区拼接成一个大分区，该操作称为**紧凑**。

- 紧凑需要动态重定位硬件支持，进程移动后仅需修改基地址寄存器
- 缺点：紧凑过程CPU开销大，会导致系统卡顿

#### 内存保护

通过两个硬件寄存器实现：

- **界限寄存器**：记录进程逻辑地址的最大范围，超出则触发越界异常
- **重定位寄存器**：存储进程的物理基地址，用于逻辑地址到物理地址的转换

------

## 六、分页存储管理

连续分配的外部碎片问题严重，紧凑开销过高，因此引入离散分配方式，分页是其中最核心的方案。

### 6.1 核心基础概念

1. **页帧（物理页/页框）**：物理内存被划分为大小相等的固定块，是内存分配的最小单位，每个块有唯一页帧号
2. **页（虚拟页/页面）**：进程的虚拟地址空间划分为与页帧大小相同的块，每个页有唯一虚拟页号
3. **页内偏移**：页内的字节偏移量，虚拟页和物理页帧的页内偏移完全一致
4. **页表**：内核为每个进程维护的映射表，数组下标为虚拟页号，元素为页表项（PTE），记录对应物理页帧号和控制位

### 6.2 地址转换与MMU

CPU中的**内存管理单元（MMU）**自动完成虚拟地址到物理地址的转换，一级页表的转换流程：

1. 拆分虚拟地址为「虚拟页号 + 页内偏移」
2. 虚拟页号与页表长度比对，越界则触发异常
3. 计算页表项物理地址：页表基地址（PTBR寄存器） + 虚拟页号 × 页表项大小
4. 读取页表项，若有效位为1，取出物理页帧号
5. 物理页帧号拼接页内偏移，得到最终物理地址

### 6.3 内存保护

页表项中增加权限控制位（读权限、写权限、执行权限等），非法访问会触发保护故障（段错误）；同时通过有效位标记页面是否分配物理内存，访问无效页面会触发异常。

### 6.4 快表（TLB）

#### 背景问题

一级页表模式下，一次数据访问需要两次内存读写（读页表+读数据），访存性能下降约50%。

#### TLB原理

- TLB（翻译后备缓冲器）是MMU内部的高速缓存，缓存高频的「虚拟页号→页表项」映射
- 由高速SRAM组成，虚拟页号直接检索，命中时1个时钟周期即可得到页帧号，无需访问内存
- 未命中时才访问内存中的慢表，并将结果回填TLB

#### 有效访问时间（EAT）

公式：`EAT = 命中率×(快表访问时间 + 内存访问时间) + 未命中率×(快表访问时间 + 2×内存访问时间)` TLB命中率越高，访存性能越接近原生内存速度。

### 6.5 多级页表

#### 背景：一级页表的内存缺陷

32位系统4KB页面下，单进程页表需4MB连续内存，且大量无效页表项被浪费；64位系统下一级页表体积更是大到无法实现。

#### 核心思想

将页表离散存储，只保存有效的页表项，无效虚拟地址不分配页表空间，以时间换空间。

#### 两级页表（32位系统）

- 虚拟地址结构：`10位页目录号 + 10位页号 + 12位页内偏移`
- 一级页表（页目录）常驻内存，共1024个目录项，每个目录项指向一个二级页表
- 每个二级页表占1个物理页帧，包含1024个页表项
- 转换流程：页目录号索引页目录→得到二级页表页帧号→页号索引二级页表→得到物理页帧号→拼接偏移
- 收益：未使用的虚拟地址段无需创建二级页表，可节省90%以上的页表内存

#### Linux多级页表实现

- 32位系统：两级页表，内核页表全局共享，每个进程无需重复存储
- 64位x86-64系统：
  - 四级页表：PGD（页全局目录）→ PUD（页上级目录）→ PMD（页中间目录）→ PTE页表，使用48位虚拟地址，支持256TB寻址
  - 五级页表：新增P4D层级，使用57位虚拟地址，支持128PB寻址
- 权衡：级数越多页表内存越小，但地址转换访存次数越多；实际依靠TLB高命中率抵消性能损耗

### 6.6 分页管理的优缺点

- 优点：无外部碎片，内存利用率高；支持离散分配，无需内存紧凑
- 缺点：存在内部碎片；页表占用额外内存；地址转换增加访存开销

------

## 七、分段存储管理

### 7.1 分页的局限性

分页以物理块为单位，不感知程序逻辑结构。当同一个页面同时包含只读代码和可写数据时，无法做精细化权限控制，也难以按逻辑模块实现代码共享。

### 7.2 分段核心原理

- 将进程虚拟地址空间**按逻辑功能划分为多个大小不等的段**（如代码段、数据段、堆段、栈段、子程序段），每个段有唯一的段号
- 物理内存以段为单位离散分配：单个段内部占用连续物理内存，段与段之间可以不相邻
- 虚拟地址是二维结构：`段号 + 段内偏移`，符合程序员的逻辑认知

### 7.3 段表与地址变换

- 每个进程维护一张段表，表项包含：段长、段基地址、读写执行权限位
- CPU设有段表寄存器，保存段表的物理基地址和总长度
- 地址转换流程：
  1. 段号与段表长度比较，越界则触发异常
  2. 计算段表项地址，读取段基地址和段长
  3. 段内偏移与段长比较，越界则触发异常
  4. 段基地址 + 段内偏移 = 最终物理地址

### 7.4 分段管理的优势

1. 方便编程：按逻辑模块划分地址，结构直观易读
2. 信息共享：以完整逻辑段为单位共享，比分页更简单自然
3. 内存保护：以逻辑段为单位设置权限，保护粒度更精准
4. 动态链接：运行时按需加载目标段，天然适配分段模型
5. 动态增长：数据段、栈段可按需扩容，无需预先分配固定空间

### 7.5 分段管理的缺点

- 存在外部碎片，段换入换出后会产生零散空闲内存
- 段长不固定，内存分配与管理复杂度高于分页

------

## 八、段页式存储管理

### 8.1 核心设计思想

结合分页与分段的优势：**用户地址空间先按逻辑功能分段，每个段内部再按固定大小分页**；物理内存始终以页帧为单位分配管理。

- 保留分段优势：逻辑清晰、易共享、易保护、适配动态链接
- 继承分页优势：无外部碎片、内存利用率高

### 8.2 地址结构

以32位虚拟地址为例：`5位段号 + 15位段内页号 + 12位页内偏移`

### 8.3 地址变换过程

系统为每个进程维护一张段表，每个段对应一张独立页表：

1. 段号与段表长度比较，越界则异常；通过段号索引段表，得到该段的页表基地址和页表长度
2. 段内页号与页表长度比较，越界则异常；通过页号索引页表，得到物理页帧号
3. 页帧号拼接页内偏移，得到最终物理地址

### 8.4 特点

- 理论上一次数据访问需3次内存访问（段表→页表→数据），实际依靠TLB缓存抵消性能损耗
- 是现代主流操作系统广泛采用的内存管理模型

------

## 九、章节核心总结

内存管理的演进，本质是内存利用率、实现复杂度与访存性能的持续权衡：

1. **连续分配**实现简单，但外部碎片严重，仅适用于早期简单系统
2. **分页管理**彻底解决外部碎片，内存利用率高，但逻辑粒度较粗
3. **分段管理**适配程序逻辑结构，易共享、易保护，但存在外部碎片
4. **段页式管理**集两者之长，是现代操作系统的主流方案
5. 地址转换依赖硬件MMU与软件页表协同，通过TLB、CPU Cache等多层缓存弥补访存性能损耗，在内存利用率和执行效率间取得平衡
